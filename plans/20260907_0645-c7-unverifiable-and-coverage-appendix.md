# C7 appendix — the 47 UNVERIFIABLE claims, and what each unit verified clean

**Why this file exists.** `KB_GAPS.md` instructs a future pass to *"drop entry #29"* and
*"re-run #13/#33/#34/#44"*. Those numbers were meaningless without this list, which existed
only inside a workflow transcript — the exact handoff failure `CLAUDE.md` warns about
(*research held only in a workflow transcript is lost*). Caught during the pre-clear check
for step 11 and written to disk here.

**Numbering is the 1-indexed order below** — the same order the completeness critic read.

---

## 1. The 47 UNVERIFIABLE claims

These are **not** false. They are claims no fetch settled. This repo's rule is to report
UNVERIFIABLE rather than assert absence, so each one is parked, not dropped. Before
re-searching any of them, read what was already tried.

| # | Unit | Claim | What was tried |
|---|---|---|---|
| 1 | `24[351-700]` | docs/24-harness-patterns.md:584-586 — the @steipete tweet (https://x.com/steipete/status/2074638582418231495) describing an 'Advisor loop' where 'an executor session calls a stronger model only on demand for guidance, while a cheaper model does the bulk implementation work'. | curl -sL on the tweet URL returns HTTP 200 but the page is a client-side-rendered SPA shell with no tweet text in the HTML (X/Twitter blocks server-side content extraction without authenticated login). Tried a web search for the described content; it surfaced Anthropic's own 'Advisor tool' feature (a related but distinct, officially-documented pattern) and third-party coverage of it, but nothing t |
| 2 | `24[1-350]` | Karpathy tweet (docs/24-harness-patterns.md:27-28), status ID 2069822834160124091: "It is an org-level harness. The difference will become clearer over time." on Claude Tag, Jun 2026. | Fetched x.com/karpathy/status/2069822834160124091 directly (returned 404 'Post Not Found'), queried the api.fxtwitter.com mirror for the same ID (returned {code:404, tweet:null}), and checked the Wayback Machine availability API for both x.com and twitter.com variants of the URL (no archived snapshots). A WebSearch for the quote text found secondary corroboration of the wording and context (Karpat |
| 3 | `24[1-350]` | "the bench runs against three transport drivers (in-process SDK, full server, native TUI)" (docs/24-harness-patterns.md:124-125), attributed to omnigent-ai/omnigent's harness-bench. | Ran `gh api search/code` for 'in-process SDK' and 'native TUI' scoped to omnigent-ai/omnigent; got dozens of hits referencing native-TUI bridges generally (kiro, hermes, qwen, codex, etc.) but none pinning down a harness-bench-specific 'three transport drivers' framing, and the GitHub search API's 30/min rate limit was hit repeatedly before an exhaustive pass could complete. |
| 4 | `24[1-350]` | "Bounded review-fix loop: capped at a maximum of 5 iterations with mechanical (not self-assessed) verdicts" (docs/24-harness-patterns.md:171-172), attributed to KaolaBrother/Kaola-Workflow's Task-Shaped DAG orchestration. | Read docs/architecture.md in full (no iteration-cap language found there) and searched the repo for '5 iterations', 'max_iterations', 'MAX_REVIEW', and 'review-fix' via `gh api search/code`. The only 5-related hit found was an unrelated rejected proposal ('LOOP_CAP (5) is an order of magnitude too small') for a different feature (the metric-optimizer's OPT budget, which actually uses `OPTIMIZE_ITE |
| 5 | `09[141-365]` | The CLI version under which the --disallowedTools vs --allowedTools distinction (docs/09-headless-mode.md:271-279) was originally tested in production, on or before 2026-07-06 (commit 8c629aba). | Read commit 8c629aba's message and full diff; grepped .claude/skills/fetch-loop-news/SKILL.md and .claude/skills/integrate-loop-news/SKILL.md for a version number near the disallowedTools discussion; grepped the upstream CHANGELOG.md for disallowedTools entries near that date. None records a CLI version for the original test. I re-verified the behavior holds on the currently installed 2.1.263 (see |
| 6 | `24[701-1000]` | docs/24-harness-patterns.md:883 — "**Compaction persistence** — context compaction events are persisted alongside the session state. When a session resumes (`claude --resume`), the harness replays the compaction log to reconstruct the effective context without requiring the agent to re-read all prio | Fetched omnigent/runtime/compaction.py directly (raw.githubusercontent.com, main) and grepped for 'resume'/'persist'/'replay' — found 'persist' and 'replay' used in unrelated contexts (synthetic summary items, /compact behavior) but nothing tying compaction persistence specifically to resume-latency reduction. Fetched omnigent's CHANGELOG.md and found real resume+compaction bug-fix entries, but th |
| 7 | `24[701-1000]` | docs/24-harness-patterns.md:748-757 — the ten-item 'Ten Principles of Harness Engineering' list attributed to Harness Books / AgentWay, chapter 9, matches the source chapter's actual ten principles term-for-term. | Fetched https://harness-books.agentway.dev/book1-claude-code/chapter-09-ten-principles.html (HTTP 200, confirmed live) and stripped tags to plain text. The chapter is written in Chinese (zh-Hans); I grepped for a set of guessed Chinese renderings of the KB's English principle phrases ('不稳定组件/同事', '工作记忆', '自己的作业', etc.) and got very few matches (only '心跳' heartbeat and '控制面' control plane hit). Thi |
| 8 | `08[251-487]` | ([wquguru/harness-books](https://github.com/wquguru/harness-books), AgentWay, Jun 2026.) at line 292 — attribution of the specific allow/ask/deny JSON example (git commit/push patterns) to this exact source. | Confirmed the repo exists (gh api repos/wquguru/harness-books) and that chapter-08-team-landing-practices.md does discuss the same read/write/irreversible risk-tiering concept (risk_tier: read \| write \| irreversible, an approval-rule template with allow\|deny\|ask decisions) in Chinese — the concept checks out as a genuine match. I did not locate the exact allow/deny/ask JSON snippet with the sp |
| 9 | `08[251-487]` | ([cobusgreyling/loop-engineering](https://github.com/cobusgreyling/loop-engineering), Jun 2026.) at line 319 — attribution of the specific safety path denylist (.env, secrets/**, auth/**, payments/**, migrations/**, k8s/production/**, credentials/**) to this repo. | Confirmed the repo exists and its description (loop-audit, loop-init, loop-cost CLI tools) is consistent with a permissions/denylist topic. Attempted `gh api search/code` for 'denylist' and related terms scoped to this repo but hit GitHub's secondary rate limit (403, 'API rate limit exceeded... search') twice in a row and did not have remaining budget to retry after the cooldown and re-verify the  |
| 10 | `08[251-487]` | ([@akshay_pachaar](https://x.com/akshay_pachaar), DailyDoseofDS, Jun 2026.) at line 348 — attribution of the Reject+Replan pattern / prompt-addition text to this specific X post. | X/Twitter posts are not fetchable via plain curl (requires JS rendering / auth and returns a login wall or empty shell to unauthenticated scrapers); I did not have a browser tool loaded for this pass. Could not confirm the handle exists or that it posted this specific content. |
| 11 | `08[251-487]` | > "before you trust an agent to run on its own, do these 4 things" — [@Sabrina_Ramonov](https://x.com/Sabrina_Ramonov), Jun 2026 at line 366 — verbatim quote and attribution. | Same limitation as above: X posts are not retrievable via curl in this environment. Could not verify the quote is verbatim or that the account exists/posted it. |
| 12 | `34[171-344]` | Token Cost Reference table (lines 171-176: Noop pass ~3,000-5,000; Report-only triage ~50,000-80,000; Action run ~200,000-250,000; CI Sweeper at 5 min cadence ~5M/day) and the per-pattern token-cost estimates later in range (Debt Audit ~80,000-120,000; Docs Sync ~50,000-100,000) are presented as 'th | Fetched cobusgreyling/loop-engineering's docs/operating-loops.md in full and grepped faisalishfaq2005/loopflow's templates/*.yaml for cost figures; found order-of-magnitude agreement but no exact-match source table for the consolidated 'Token Cost Reference' section or the loopflow-specific per-pattern estimates. Since the section is explicitly disclaimed as generic operating estimates rather than |
| 13 | `07[1-230]` | docs/07-subagents.md:101 'Grades against intent rather than observable behaviour' and lines 103-106's three-item 'fix' list (show a divergent example, require evidence not verdicts, fail loudly with no partial credit) are attributed, along with the surrounding bullets, to '(Anthropic Engineering, Ma | Fetched and full-text-searched both https://www.anthropic.com/engineering/harness-design-long-running-apps and https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents (curl + tag-strip) for 'intent', 'observable behav', 'evidence', 'stack trace', 'reproduction', 'verdict', 'partial credit', 'fail loudly' — no hits in either. Also pulled the full list of Anthropic Engineering article |
| 14 | `32[1-180]` | docs/32-reading-list.md:56 cites https://medium.com/p/loop-engineering-designing-the-execution-harness-around-an-llm-936afeb6a72d for Roan Brasil Monteiro's article, and its content (0.9^10=35% compound-probability example, the 'mediocre model inside a well-engineered loop' quote) — I could not fetc | curl (plain and with a spoofed Chrome User-Agent) returned HTTP 403 from a Cloudflare challenge page both times; WebFetch also returned 403; no Wayback snapshot exists for this URL (checked via the availability API — empty archived_snapshots). I substantiated the claim instead via WebSearch, which surfaced the article at its canonical https://medium.com/@roanmonteiro/... URL (same post ID 936afeb6 |
| 15 | `33[1-190]` | docs/33-agent-security-hardening.md:53 — 'The agent reads SECURITY_MATRIX.md at startup and self-assesses ambiguous instructions before executing.' (automatic loading of SECURITY_MATRIX.md into agent context at session start) | Confirmed SECURITY_MATRIX.md exists verbatim in jahwag/clem (root of repo) with matching content — the known-safe `kill $PPID` example and known-attack-patterns framing this doc describes are accurate quotes/paraphrases of the real file, including a cross-reference to 'CLAUDE.shared.md § How your session ends'. But I could not confirm the *mechanism* by which it gets read at agent startup: `gh api |
| 16 | `33[1-190]` | docs/33-agent-security-hardening.md:21 — the specific illustrative directory pattern '`/home/agent-<id>/`' as clem's actual per-agent home-directory naming | clem's own threat model (docs/threat-model.md) names the actual OS-user convention as `<project>-<agent>` (e.g. table row: '**agent UID** (`<project>-<agent>`)'), not `agent-<id>`. I did not do a full source-level search to determine whether `/home/agent-<id>/` is ever literally used (e.g. as a doc example elsewhere in clem) versus being the KB author's own generic illustration of the general patt |
| 17 | `32[361-520]` | docs/32-reading-list.md:419-421 — Gusto bug-fixing runs 'an eval-first loop (write a reproducing eval/test, fix, re-run as the stopping condition, gate on human review before commit)'. | Fetched the full cited Lenny's Newsletter page (curl -sL, HTTP 200). It is a podcast-episode landing page whose visible text is show notes/chapter markers, not a transcript. The show notes confirm the general framing — 'The eval-first workflow Eddie uses to fix real customer bugs with Claude Code' and a chapter marker 'Live coding with evals in Claude Code (39:39)' — but the specific mechanics (re |
| 18 | `32[361-520]` | docs/32-reading-list.md:478-479 — session-orchestrator's '/evolve + /reconcile commands enable governed cross-session pattern extraction where no rule auto-applies without human review.' | Fetched the live README (gh api repos/Kanevry/session-orchestrator/readme). Found strong supporting language ('Cross-session learning is opt-in and inspectable... after 5+ sessions /evolve analyze extracts confidence-scored patterns you can read and prune. Nothing is hidden'; '/reconcile' listed among the 25 slash commands; 'the reconcile engine still dedupes' on provenance markers) but no single  |
| 19 | `12[1-160]` | Exact date/version at which Claude Code's `prompt` hook type (type: "prompt") stopped being labeled experimental, if it ever was (docs/12-hooks.md:17). | Searched the full raw CHANGELOG.md for 'prompt hook', 'prompt-based hook', and 'type: "prompt"' — no matching entries found. Fetched the current https://code.claude.com/docs/en/hooks page and confirmed only agent hooks carry an 'experimental' label today; found no Wayback or dated-snapshot mechanism available to check the page's state on the doc's 2026-06-23 authoring date. |
| 20 | `34[1-170]` | Whether the exact token-cost ranges quoted for each pattern (Daily Triage ~50,000–80,000; PR Babysitter ~200,000–250,000; Post-Merge Cleanup ~50,000–80,000, etc.) are meant to represent "typical active run" cost vs. "full run including no-ops," and whether that framing is itself sourced or an indepe | Compared each pattern's Cost Profile against the equivalent scenario in cobusgreyling/loop-engineering's per-pattern docs (patterns/daily-triage.md, pr-babysitter.md, post-merge-cleanup.md, dependency-sweeper.md, changelog-drafter.md — all fetched via gh api). The KB's ranges are in the right order of magnitude and roughly track the source's "triage/scan" and "fix/L2" rows, but the KB doesn't cite |
| 21 | `05` | docs/05-claude-md.md:103 — 'Circular imports are ignored.' (in the @import syntax section) | Checked the current code.claude.com/docs/en/memory page in full (confirms 'maximum depth of four hops' but says nothing about circular imports specifically — the only 'circular' hit on that page is about circular *symlinks* in .claude/rules/, an unrelated mechanism). Checked the Wayback snapshot closest to this line's 2026-06-23 commit date (2026-06-22 and 2026-06-25 snapshots) — same result, no m |
| 22 | `05` | docs/05-claude-md.md:126-137 — '## Customizing compaction' section: that CLAUDE.md supports a special '# Summary instructions' heading that 'tells the compactor what to preserve when context is summarized', read automatically by Claude Code's compaction step. | Fetched and fully read code.claude.com/docs/en/context-window's dedicated 'What survives compaction' section, which gives a complete mechanism-by-mechanism table (system prompt, CLAUDE.md, auto memory, plan-mode plan, path-scoped rules, nested CLAUDE.md, read/edited files, skill bodies, hook-added context, SessionStart hooks) and does not mention a '# Summary instructions' heading or any similar C |
| 23 | `12[161-304]` | docs/12-hooks.md:161-169 — the exact Claude Code version that introduced `asyncRewake` (needed to satisfy Part II's version-stamp promise for the asyncRewake circuit-breaker script in this range). | Fetched the full raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md and grepped for 'asyncRewake', 're-wake', 'rewake', and 'wake Claude' — zero hits. The field's behavior is confirmed correct against current docs and against the 2026-06-24 Wayback snapshot, but its introducing changelog entry is either phrased without those keywords or predates the fetched changelog's coverage; di |
| 24 | `12[161-304]` | docs/12-hooks.md:262-268 — version of the 'Verify-before-stop' asyncRewake loop pattern (same root cause as above: asyncRewake's own introduction date is unresolved). | Same CHANGELOG.md search as above; no dated entry found. |
| 25 | `12[161-304]` | docs/12-hooks.md:270-275 — version in which PreToolUse hooks gained the ability to return `hookSpecificOutput.permissionDecision: "deny"` (the mechanism the 'block destructive commands' pattern relies on). | Grepped CHANGELOG.md for 'permissionDecision' and 'PreToolUse'; found many later refinements (e.g. precedence rules, `ask` floor for auto mode) but not a clear single 'added' line — the capability looks foundational to the original hooks feature, which may predate the changelog's earliest covered versions (file's oldest entries: 0.2.21). |
| 26 | `12[161-304]` | docs/12-hooks.md:197 — the Claude Code version that introduced `CLAUDE_ENV_FILE`. | Grepped CHANGELOG.md for 'CLAUDE_ENV_FILE' — found three later fix/behavior-change entries (v2.1.111 Windows support, a comment-parsing fix) but no 'Added CLAUDE_ENV_FILE' line; its original introduction likely predates those or uses different wording. |
| 27 | `12[161-304]` | docs/12-hooks.md:195-196 — the Claude Code versions that introduced `CLAUDE_CODE_REMOTE` and `CLAUDE_CODE_REMOTE_SESSION_ID`. | Grepped CHANGELOG.md for both variable names — no 'Added' line found for either; content of both rows is otherwise confirmed correct against code.claude.com/docs/en/env-vars. |
| 28 | `12[161-304]` | docs/12-hooks.md:293-297 — version in which the `SubagentStop` hook event was introduced (needed to version-stamp the 'chain background agents' pattern). | Grepped CHANGELOG.md for 'SubagentStop' — found refinements (Stop/SubagentStop `additionalContext` support at a later version, `background_tasks` field added) but not the event's original introduction; likely foundational and predates the changelog's covered range. |
| 29 | `07[231-464]` | docs/07-subagents.md:352-353 — 'Forked subagents (`subagent_type: "fork"`) inherit the full parent context and prompt cache — ideal when the subtask needs all the context the parent has built up.' This is an old (2026-06-23) Part II platform fact with NO version marker, which is a defect against the | Fetched code.claude.com/docs/en/sub-agents and confirmed the behavior is current and accurate. Searched the CHANGELOG.md for the introduction of prompt-cache-sharing-on-fork specifically and did not find a single dated entry naming that exact guarantee (only entries about /fork's UI behavior and fork-mode defaults, which describe surrounding mechanics, not the cache-sharing claim itself). |
| 30 | `07[231-464]` | docs/07-subagents.md:342 — '\| v2.1.172 – v2.1.216 \| 5, and **not changeable** \|' (part of the Version history table, authored 2026-09-04, so not in this unit's old-priority set, but checkable). | Grepped CHANGELOG.md for the introduction of the depth-5 default around v2.1.172 and did not find an explicit changelog line stating it (the changelog's visible history doesn't cover a dedicated entry for that original default — plausibly because it was never 'added' by a changelog-worthy commit, just the shipped default). The two flanking rows (v2.1.217-218 depth 1, v2.1.219+ depth 3) ARE changel |
| 31 | `28` | The exact date or Claude Code / claude.ai version at which the 'Allow unrestricted branch pushes' permission toggle was replaced by the current automatic three-condition branch-push check (protected / existing PR / third-party commits). | Searched raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md for 'branch', 'routine', and 'unrestricted' — no hits. This appears to be a claude.ai web-UI-side change (account/organization policy, not a CLI release), so it likely isn't tracked in the CLI's CHANGELOG.md at all. Did not bisect the full Wayback Machine snapshot history between 2026-06-16 and 2026-09-07 to find the trans |
| 32 | `28` | Whether 'ready_for_review' is offered as a specific, individually selectable GitHub PR trigger action in the current claude.ai/code/routines web UI dropdown (as opposed to being covered only by the generic 'otherwise updated' catch-all in the docs' summary table). | Read the full 'Supported events' section and 'Filter pull requests' section of code.claude.com/docs/en/routines (current and the 2026-06-16 capture) — the docs describe the category in prose ('opened, closed, assigned, labeled, synchronized, or otherwise updated') rather than enumerating every selectable UI action value, and note a separate 'Is draft' filter field for scoping to ready-for-review P |
| 33 | `01` | Line 25: Ng's 'one first-party figure for unattended running is "around an hour"' (attributed via the internal link to docs/14-human-in-the-loop.md rather than a direct external URL in this doc). | Confirmed docs/14-human-in-the-loop.md exists, but per my assigned unit (docs/01 lines 1-120 only) I did not fetch Ng's underlying source to verify the 'around an hour' figure itself — that citation lives in doc 14, out of scope for this pass. Doc 01 itself carries no external URL for this specific figure to check. |
| 34 | `01` | Exact verbatim wording of the two blockquotes at lines 58-59 and 61 ('The performance ceiling...' and 'A mediocre model...') as they appear inside Roan Brasil Monteiro's Medium article. | Direct curl of https://medium.com/@roanmonteiro/loop-engineering-designing-the-execution-harness-around-an-llm-936afeb6a72d returned a Cloudflare 'Attention Required!' block page, not article content. I corroborated both quotes only indirectly, via WebSearch's indexed snippets of the same URL, which reproduced matching text. I could not do a direct character-for-character verbatim check. |
| 35 | `01` | Whether the second blockquote at line 61 ('A mediocre model inside a well-engineered loop outperforms a frontier model invoked once.') is itself from the roanbrasil/Monteiro piece, or from some other source — it carries no attribution of its own, only relying on the section-closing citation at line  | A WebSearch summary attributed this exact sentence to the same Monteiro article alongside the first quote, but I could not independently confirm via direct fetch (Cloudflare-blocked, as above). |
| 36 | `26` | docs/26-factory-model.md:137 citation label '(@thorstenball repost via @steipete...)' — whether @steipete specifically reposted/retweeted Thorsten Ball's tweet (as opposed to the KB author simply linking Thorsten Ball's own tweet). | Fetched the tweet via publish.twitter.com/oembed (confirms author=Thorsten Ball, date=2026-07-07, text matches the KB's characterization of the discussion) and via a raw x.com page fetch (JS-rendered SPA, no usable content in raw HTML, no 'steipete' string present). X/Twitter's retweet/quote-tweet chain data isn't accessible without an authenticated API or JS-rendered browser session, so I could n |
| 37 | `26` | docs/26-factory-model.md:103 — exact publish date of the OpenAI article 'How agents are transforming work', cited as 'Jul 2026'. | Direct curl to openai.com returned HTTP 403 (Cloudflare bot challenge) both with and without a browser User-Agent. Retrieved full article text via the r.jina.ai reader proxy instead, which confirmed both quoted statistics verbatim (99% of engineer output tokens via Codex; 99th-percentile users regularly exceeding 60 hours of parallel Codex agent turns/day) but carried no publish-date metadata. The |
| 38 | `09[1-140]` | The exact Claude Code version that introduced `--no-session-persistence` (line 76) — needed to give it a version stamp under the Part II promise. | Grepped the full CHANGELOG.md (6362 lines, back to v0.2.21) for `no-session-persistence`, `session-persist`, `ephemeral`, and `SKIP_PROMPT_HISTORY` — no matching "Added ..." entry under any of those names. Confirmed the flag exists and works exactly as the doc describes via the current cli-reference.md ("Print mode only"), but its introduction version isn't recorded in the changelog under any name |
| 39 | `09[1-140]` | docs/09-headless-mode.md:120-121 — "Verified on `claude` **2.1.261**" for the --print-only guarantee around --permission-prompts / --max-budget-usd / --bg. | Confirmed the underlying claims (the --bg/--print conflict, the --max-budget-usd "(only works with --print)" help text, and the exact "Who answers permission prompts with --print" wording) are all accurate against the currently installed claude 2.1.263 and the official cli-reference. I have no way to confirm that the specific patch version 2.1.261 (rather than some other nearby version) was what t |
| 40 | `13` | docs/13-context-management.md:112-113 — 'Microcompact \| Compress completed subtask summaries into one-line records' and 'Collapse \| Merge redundant assistant-turn repetitions', both cited to (wquguru/harness-books, AgentWay, Jun 2026). | Searched the full wquguru/harness-books repo (gh api search/code for 'microcompact') and read every chapter that surfaced — chapter-01-why-harness-engineering.md, chapter-03-query-loop-heartbeat.md, chapter-05-context-memory-compact.md, chapter-06-errors-and-recovery.md, plus the diag-b04-01-context-sources-and-budget.puml diagram. All of them name 'microcompact' and 'collapse' as steps in the gov |
| 41 | `08[1-250]` | Whether the `?` (single-character) wildcard token in docs/08-permissions.md:245 was ever a documented/supported Claude Code permission-rule pattern that was later silently removed from the docs, versus never having existed. | Fetched and read the full, current 'Configure permissions' page (code.claude.com/docs/en/permissions) end to end — every section documenting wildcard syntax for Bash, PowerShell, Read/Edit, WebFetch, MCP, Agent, and Cd rules — and found no `?` token anywhere. Also checked the Settings reference page (no wildcard mentions at all). Searched the complete raw CHANGELOG.md (6362 lines) for any entry ad |
| 42 | `21` | docs/21-context-vs-loop-engineering.md:14 — 'Context engineering is primary ([@techtasium](https://x.com/techtasium) and others)' — that this specific X account holds/argued this specific position. The citation link itself satisfies rule 7 (it links the @handle to a real-looking profile URL), but I  | WebSearch for 'techtasium "context engineering"' and 'techtasium twitter context engineering agent' — no indexed results for this handle at all (search engines largely can't index individual X/Twitter posts). Tried Wayback Machine's availability API for both x.com/techtasium and twitter.com/techtasium — both returned zero archived snapshots (archived_snapshots: {}), which is inconclusive (Wayback  |
| 43 | `31` | Line 53-54: Boris Cherny (Claude Code's creator) frames Claude Tag as "Claude Everywhere": loops embedded across all team tools, not just the terminal. — the specific quote/framing and its source. | Fetched and text-searched the Anthropic Claude Tag announcement, the claude.com/tag product page, and the code.claude.com/docs/en/claude-tag doc (no hits). Ran Bing and DuckDuckGo searches for '"Boris Cherny" "Claude Everywhere"' and '"Claude Everywhere" bcherny' (DuckDuckGo returned only a CAPTCHA; Bing returned no on-topic results across three query variants). Ran `gh api search/code` for the ex |
| 44 | `19` | Whether `--allowedTools`'s 'pre-approval only, does not restrict' semantics (used in finding 1) were identical on the doc's exact capture date, 2026-06-21, versus current (2026-09-07). | Fetched the current live CLI reference (code.claude.com/docs/en/cli-reference) and cross-checked this KB's own docs/10-fan-out.md, which states the same fact and quotes the same CLI-reference sentence. Checked the Claude Code CHANGELOG.md for allowedTools/disallowedTools history (the `/allowed-tools`→`/permissions` rename and `--disallowedTools` addition both appear far earlier in the changelog th |
| 45 | `17` | docs/17-failure-patterns.md:43 — 'Engineers who copy-paste AI output score under 40% on comprehension tests vs 65%+ for those using AI assistively (Osmani, May 2026).' Is this a real, correctly-attributed Osmani statistic? | Fetched (curl + strip-tags) https://addyo.substack.com/p/agentic-skill-decay in full — the only Osmani source this KB paragraph links — and searched for '40%', '65%', 'comprehension', 'copy-paste'; none present. Tried https://addyosmani.com/blog/ and https://addyosmani.com/blog/vibe-coding/ (latter returned an anomalously small 3.4KB page, suggesting a wrong/dead path, not the real article). Did n |
| 46 | `17` | docs/17-failure-patterns.md:41 — whether the tweet at https://x.com/limalemonnn/status/2069039311584891122 still contains the exact wording quoted in LOOP_ENGINEERING_NEWS.md:1859 ('$47,000 in 11 days...'). | curl -sL to the tweet URL returns HTTP 200 (not a 404 — the URL is alive) but X's SPA shell renders no text server-side. Tried the syndication API (cdn.syndication.twimg.com/tweet-result?id=...) which returned an empty '{}' — could mean the tweet is protected/deleted/rate-limited, not necessarily gone. No Wayback check attempted (out of budget for a non-priority sub-question); the underlying findi |
| 47 | `35` | Line 100, "[Dynamic workflows](39-dynamic-workflows.md) \| Scripted fan-out over many agents, in background \| supervising" has no version marker, which is arguably the same Part II promise violation as the /goal, /loop, agent-teams, and /usage rows in the same table. | Grepped the full CHANGELOG.md for an "Added ... Workflow tool" or "Added dynamic workflows" introducing entry (searched "Dynamic workflow", "Workflow tool", "Research Preview.*workflow"); only found later refinement entries (workflowSizeGuideline setting, OTel attributes, keyword-trigger setting) with no clean first-introduction line in the fetched changelog window, so I could not confidently name |

**Known dispositions, from the completeness critic:**

- **#29** — out of scope by its own text (the line it names was authored 2026-09-04, i.e.
  after the cutoff this sweep covers). **Drop it.**
- **#13, #33, #34, #44** — re-run with the Wayback fallback that settled the OpenAI citation
  date: `curl` a publisher that 403s, then `archive.org/wayback/available`. A source that
  403s where you looked is not a source that is gone.
- **#22, #25, #26, #27, #37** — genuinely changelog-silent (versions introducing
  `asyncRewake`, `CLAUDE_ENV_FILE`, `CLAUDE_CODE_REMOTE*`, `SubagentStop`,
  `--no-session-persistence`). The full upstream changelog carries *fix* entries for these
  but no *introduction* entry. Defensible — but each should state that, rather than leaving
  five identically-shaped entries with no record of what was searched.
- **#40** (`?` wildcard in `docs/08`) — **settled 2026-09-07 and closed.** It had been logged
  UNVERIFIABLE *and* proposed as a high-severity removal at the same time, which under this
  repo's rule 9 resolves to cannot-tell. Resolved properly against Wayback snapshots of the
  permissions reference from 2026-06-03, 2026-07-03 and 2026-08-03: all document only `*`
  and `**`, spanning the whole period the KB has made the claim.

---

## 2. What each unit checked and found CORRECT

This is the half of a sweep that normally gets thrown away. It is what stops the next pass
re-verifying 13 arXiv citations, the Terminal-Bench figures, or the Anthropic harness
articles. **Read the row for a doc before re-checking it.**

Coverage caveat, stated because the numbers invite over-reading: claim density ran
**inverted against document size** (r = -0.64). A unit's `verifiedClean` note is a record of
what it *did* check, not a guarantee that its range is clean — `docs/24` in particular was
sampled at 11.8 claims per 100 unverified lines against a 26.5 small-unit rate.

### `24[351-700]` — docs/24-harness-patterns.md (Part I)

302 previously-unverified lines · 19 claims checked · 14 URLs opened · 4 findings · **6.3 claims per 100 unverified lines**

Checked and found accurate, with sources fetched: (1) the Harness-vs-Environment table and the three deployment-pattern definitions (Approval-First/Curated Allow-list/Sandboxed Full-Auto) against hidekazu-konishi's article — table content matches, only the quote (finding above) and the graduation-rule wording (finding above) are wrong; (2) the 'Unstable Components' quote and its file-location citation against wquguru/harness-books' chapter-09-ten-principles.md — verbatim match, confirmed NOT in the README (as the citation itself notes); (3) the Two-Part Harness (Initializer/Coding Agent) description against Anthropic's 'Effective Harnesses for Long-Running Agents' — structured JSON feature list, browser automation via Puppeteer MCP, and git-commit-based recovery all confirmed; (4) the 'Official Claude Team Loop Types' table (Turn-based/Goal-based/Time-based/Proactive, including the '/loop runs locally, /schedule moves it to the cloud' parenthetical) against claude.com/blog/getting-started-with-loops — verbatim-level match; (5) the Opus 4.5 vs Opus 4.6 sprint-removal claim in the Three-Agent Full-Stack Harness section against Anthropic's 'Harness design for long-running application development' — confirmed both model versions are discussed and the sprint-construct-removal-on-4.6 narrative matches; (6) all six Dynamic Workflow Patterns and three named failure modes (agentic laziness, self-preferential bias, goal drift) against claude.com/blog/a-harness-for-every-task — all present with matching descriptions; (7) existence and description accuracy of professorpalmer/Puppetmaster, openai/codex-plugin-cc (including the /codex:adversarial-review command and the response-blocking review gate), Kanevry/session-orchestrator, Sungmin-Cho/claude-deep-loop (including the 'insights' kernel subcommand feeding /deep-loop init and /deep-loop-finish), and the Runtime-Republic/Constitutional-Control-Plane framing against wquguru/harness-books chapter-07 — all confirmed via gh api (default_branch checked as main for each); (8) the steipete/agent-scripts codex-first SKILL.md claims (Claude keeps design/review/destructive ops, implementation via codex exec --yolo with temp-file specs, escalation to Claude after two failed delegation rounds) — all confirmed against the live file despite the file having grown substantially since the Jul 2026 citation date; (9) the HydraFusion GitHub Copilot benchmark figures (4.9pp quality, 67% lower cost, TerminalBench 2.1, vs Claude Opus 5) against github.blog's actual post — confirmed present (this section postdates 2026-09-04 so was lower priority but checked as a specific-stat claim).

### `24[1-350]` — docs/24-harness-patterns.md (Part I)

253 previously-unverified lines · 38 claims checked · 34 URLs opened · 6 findings · **15.0 claims per 100 unverified lines**

Confirmed correct against primary sources: (1) all 13 arXiv papers cited in the old-line range (2606.10106, 2606.19613, 2606.12344, 2606.09498, 2604.25850, 2606.14249, 2606.17546, 2606.15363, 2608.25593, 2608.27311, 2608.28363, 2609.01437, 2609.01481) — titles, abstracts, and month/year all match the KB's citations. (2) Self-Harness's per-model Terminal-Bench-2.0 numbers (MiniMax M2.5 40.5%→61.9%, Qwen3.5-35B-A3B 23.8%→38.1%, GLM-5 42.9%→57.1%) verified verbatim against the paper PDF body text. (3) AHE's Terminal-Bench 2 Pass@1 69.7%→77.0%, beating Codex-CLI (71.9%), 12% fewer tokens on SWE-bench-verified, +5.1 to +10.1pp cross-family, and the tools/middleware/memory-not-prompt ablation — verified verbatim against the abstract. (4) HarnessX's +14.5% average (up to +44.0%) across five named benchmarks — verified verbatim against the abstract. (5) Claw-SWE-Bench's 19.1% (minimal adapter) vs 73.4% (full adapter) on the same GLM 5.1 backbone — verified against the abstract (the KB's \"4x swing\" is a reasonable rounding of the actual 3.84x). (6) LangChain's \"Top 30 to Top 5 on Terminal Bench 2.0 by only changing the harness\" quote and the Opus-4.6-in-Claude-Code framing — verified verbatim against the live blog post. (7) Anthropic's \"Making of Claude Code\": the \"clide\" codename, the \"tool definitions in a loop and a simple REPL UI\" quote, \"fanned out a hundred Claude Haikus in parallel\", the persistent-shell-and-bash-tool origin, and the 2022 starting point — all verified against the live article's embedded text. (8) Bun's 11-day timeline, the 64-Claude parallel fan-out, the Zig→Rust framing, and the git-stash/git-reset dangerous-command incident and its process-level fix — verified against the live blog post (only the one-word quote tense was off, reported separately). (9) The Hugging Face space's title (\"Don't Train the Model, Evolve the Harness\"), its \"matched Sonnet 4.6... at roughly 7x lower cost\" claim (verified via the underlying data.json: DeepSeek-optimized all_pass 5.0%/$77.3 vs. Sonnet 4.6 all_pass 5.4%/$546.9 → 7.08x), and the \"3.5% to 80.1%\" external-harness spread — all confirmed against the space's own chapter text and raw data file. (10) All six GitHub repos cited in the old-line range (omnigent-ai/omnigent, duct-tape-and-markdown/temper, KaolaBrother/Kaola-Workflow, ruvnet/metaharness, Aditya-Nagariya/harness-forge, nmlemus/harness-token-efficiency) exist, are active, and match their described subject matter. (11) The `PolicyDeniedEvent` class itself genuinely exists in omnigent's schemas.py, and metaharness's Darwin Mode (self-mutating harness config, sandboxed testing, strict holdout/out-of-sample promotion rule) and its companion router (@metaharness/router, \"cheapest model that clears your quality bar\", learned from eval logs) both check out against the live README and darwin-mode README. (12) The internal cross-reference docs/24→docs/27#experience-encoding resolves to a real section. (13) A rule-7 sweep of lines 1-350 found no bare/unlinked repo slugs or handles — every external reference is a markdown hyperlink.

### `09[141-365]` — docs/09-headless-mode.md (Part II)

225 previously-unverified lines · 20 claims checked · 2 URLs opened · 4 findings · **8.9 claims per 100 unverified lines**

Checked and found correct/current: (1) CLI flags used in this range all exist and behave as described on locally installed claude 2.1.263 — --max-turns, --max-budget-usd, --permission-mode auto (valid enum value), --exclude-dynamic-system-prompt-sections, --no-session-persistence, --chrome (verified via `claude --help` and the official CLI reference page fetched via curl); --max-turns specifically is undocumented in local `--help` output but confirmed live and real via the official docs page and the upstream CHANGELOG. (2) The macOS `script(1)` exit-code-masking claim (lines 163-168) matches `man script`'s own description of the `-e` flag ("the child command exit status is always the exit status of script" implying it is NOT so without -e). (3) Worktree-isolation points 1 (throwaway worktree + trap cleanup EXIT removes it in every path) and 2 (two-session artifact handoff via .loop-news/findings.json) still match scripts/run-loop-news.sh and the two skill files. (4) The "success path fast-forwards the primary checkout only if it is on main" claim (lines 243-244) matches the script's final block exactly. (5) The Pitfall section's claim that integrate-loop-news's Phase 0 checks via `git log --grep` whether this exact run was already committed, and aborts if so (lines 287-289), matches .claude/skills/integrate-loop-news/SKILL.md Phase 0 step 4 essentially verbatim. (6) The `--disallowedTools "Bash(git *),Bash(gh *),Skill"` scoping claim (lines 278-279) matches the actual invocation in scripts/run-loop-news.sh line 365 exactly. (7) All 6 internal markdown links in range (lines 121, 217, 222-225 by relative offset — i.e. docs/09 lines 361-365 "Related" section plus the inline #core-flags anchor and the 28/29 doc cross-refs) resolve to files/anchors that exist. (8) No bare repo/tool/@handle citations in range — grep for github.com and @handles in lines 141-365 found none. (9) The Routines-cannot-access-local-Chrome and LaunchAgent-preferred-for-Chrome claim (lines 354-358) cross-checked against docs/28-routines.md, which independently states the same thing ("Chrome browser automation | ... | No — no local Chrome").

### `24[701-1000]` — docs/24-harness-patterns.md (Part I)

201 previously-unverified lines · 22 claims checked · 27 URLs opened · 7 findings · **10.9 claims per 100 unverified lines**

Verified accurate/well-sourced against live + capture-date-contemporaneous fetches: (1) lines 701-703, Sungmin-Cho/claude-deep-loop v1.4.0 'insights' kernel subcommand claim — confirmed against the repo's own CHANGELOG.md ('## [1.4.0] — 2026-07-07', matching description of the insights subcommand, atomic-write path, and feed-forward into deep-loop-finish/init); repo has since been renamed claude-deep-loop→deep-loop but the old URL still 301-redirects, so not a dead link. (2) Lines 705-740, the 'Query Loop as System Heartbeat' analysis (nine state variables, query.ts:203-217, the 8-mode stop-condition matrix, the double-failure guard, api_error-no-retry) — verified word-for-word against the live, fetched harness-books.agentway.dev chapter-03 page (Chinese source text quoted and matched). (3) Lines 761-785, the NOOA / arXiv 2607.20709 / NVIDIA-NeMo/labs-OO-Agents paragraph — verified via the live arXiv abstract page, a full pdftotext extraction of the paper PDF (confirmed 82.2% SWE-bench Verified with GPT-5.5 at xhigh reasoning, and the 1.1M vs 2.2M token comparison against PI = exactly half), and the GitHub repo (Apache-2.0 LICENSE confirmed, and packages/ directory contains exactly nooa-cli, nooa-acp, nooa-memory, nooa-bench as claimed); star count (1,991 claimed vs 1,993 live) is normal day-to-day drift, not an error. (4) Lines 799-807, the luckeyfaraday/athena-loops 'lighter-weight harness-agnostic' paragraph (Agent.run(), run_id, MCP server + CLI dual exposure, cursor-based tail polling, Claude/Codex/opencode/aider backends) — verified against the repo's live README.md, matches closely including exact function/parameter names (orchestrate_status/orchestrate_tail/orchestrate_result, run_id, cursor). (5) Lines 830-833/845-848, the general 'Tenet' framing (3 independent fresh-context critics, no access to implementer's reasoning) — confirmed structurally accurate against the README's critic-pipeline description, only the phase-numbering and steer-taxonomy specifics were wrong (see findings). (6) Lines 986-990, the coleam00/archon paragraph — the verbatim quote (\"Dockerfiles for infra, GitHub Actions did for CI/CD... Archon does for AI coding workflows\") matches the live README.md exactly; forks count (3,465) matches exactly; stars (23,389 claimed vs 23,393 live) is normal drift. Repos confirmed to exist and be non-archived: Sungmin-Cho/deep-loop, luckeyfaraday/athena-loops, sergiocarvalhosa/Monad-Harness, eugenelim/agent-ready-repo, JeiKeiLim/tenet, omnigent-ai/omnigent, coleam00/archon, NVIDIA-NeMo/labs-OO-Agents.

### `08[251-487]` — docs/08-permissions.md (Part II)

188 previously-unverified lines · 14 claims checked · 9 URLs opened · 3 findings · **7.4 claims per 100 unverified lines**

Checked and found CORRECT: (1) The 'Cost of Refusal' section (lines 368-416, added 2026-09-04) — all four Andrew Ng quotes verified verbatim against the live deeplearning.ai source pages (fetched via curl, stripped HTML, exact string match including punctuation): 'Unfortunately, when we tried to perform one such review...refused to proceed beyond a certain point' and 'So we used an open agentic harness — OpenWorker itself!...to complete our security review' both from Open Models, Open Harnesses, Open Security (2026-07-31); 'the LLM refused to do so on safety grounds' from When Guardrails Go Wrong (2026-07-24); 'Guardrails on LLMs do have a place...that we're better off having models refuse' also confirmed verbatim. Both cited URLs load and match their stated titles. (2) The internal cross-reference to docs/33-agent-security-hardening.md#hardening-during-the-incident-it-exists-for (line 413-414) resolves to a real heading in that file. (3) The wquguru/harness-books citation's underlying concept (risk-tiering by reversibility: read/write/irreversible) is genuinely present in that repo's chapter-08 content, in the risk_tier field and approval-rule template — not a fabricated attribution at the conceptual level. (4) All three GitHub repos cited in this range (wquguru/harness-books, cobusgreyling/loop-engineering, omnigent-ai/omnigent) exist, are unarchived, and are on `main` as their default branch, so the raw-URL/citation links themselves are not dead. (5) Citation hyperlink formatting (Hard Rule 7) is satisfied for every external reference in this range — no bare owner/repo slugs or bare handles found.

### `32[181-360]` — docs/32-reading-list.md (Part II)

180 previously-unverified lines · 32 claims checked · 24 URLs opened · 5 findings · **17.8 claims per 100 unverified lines**

Checked 10 entries (lines 181-360), all pre-2026-09-04 per git blame (authored 2026-06-24 through 2026-07-07), against primary sources:\n\n1. Anthropic 'Harness Design for Long-Running Application Development' (183-197): author (Prithvi Rajasekaran), publish date (Mar 24, 2026), three-agent architecture (Planner/Generator/Evaluator), $9/20min vs $200/6hr cost figures — all verified verbatim against the live page and a 2026-07-01 Wayback snapshot. (Two sub-claims in this entry are wrong — see findings.)\n2. Lenny's Newsletter 'How Claude Mythos found a 15-year-old bug in Mozilla Firefox' (200-214): title, 423 fixes, ~50/50 harness/model credit split ('Brian splits the credit close to 50-50'), LLM-judge file prioritization, dedicated verifier subagent catching cheating, 'one month' timeframe — all confirmed in the fetched transcript/description.\n3. LinkedIn 'Loop Engineering...Never Argue with an Agent' by Martin Dilger (217-231): 'I never argue with an agent' rule, the verbatim closing quote 'Clean iterations with recorded learnings will outperform long, polluted conversations every single time. Not sometimes. Every time.', and the Planned/In Progress/Blocked/Done Event Modeling states — all confirmed verbatim. (One sub-claim, a quoted term, is fabricated — see findings.)\n4. 'Harness Books' by wquguru/AgentWay (234-251): repo owner and existence confirmed via authenticated gh api; 'ledger closure', the five-step pipeline in the exact order 'prefetch / snip / microcompact / collapse / autocompact', the 20,000-token summary reserve, the 13,000-token early-warning buffer, and the 3-consecutive-failure circuit breaker, and the 'runtime republic' / 'constitutional control plane' framing — all confirmed by fetching the actual English chapter source files (chapter-03, chapter-05) via gh api, not just the marketing landing page.\n5. Anthropic 'Building Effective Agents' (254-261): publish date (Dec 19, 2024) and all five named patterns (prompt chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer) confirmed against the live page's own section headers and definitions.\n6. arXiv 2604.25850 'Agentic Harness Engineering' (270-285): title, all 11 authors, and every statistic (69.7%→77.0% over 10 iterations, Codex-CLI baseline 71.9%, 12% fewer tokens on SWE-bench-verified, +5.1 to +10.1pp cross-family, ablation → tools/middleware/memory not system prompts) match the abstract exactly.\n7. Hugging Face 'Don't Train the Model, Evolve the Harness' (304-317): confirmed via the actual article chapters and underlying JSON dataset (not just the landing shell) that the benchmark is Harvey's Legal Agent Benchmark, the score range is exactly 3.5%-80.1% pooled across external harnesses, and the cost ratio Sonnet 4.6 ($546.9) / optimized DeepSeek V4 Pro ($77.3) computes to ~7.08x, matching the KB's 'roughly 7x lower inference cost' and 'matches Sonnet 4.6's score' (5.0% vs Sonnet's 5.4% all-pass).\n8. arXiv 2606.14249 'HarnessX' (321-336): title, 14 authors, substitution-algebra framing, AEGIS evolution engine, the five named benchmarks (ALFWorld, GAIA, WebShop, tau^3-Bench, SWE-bench Verified), +14.5% average / +44.0% max gains all match the abstract exactly; also confirmed docs/24-harness-patterns.md does cite this same arXiv ID, supporting the KB's 'most directly cited elsewhere in this KB' claim.\n9. github.com/ruvnet/metaharness (338-355): repo description matches ('branded CLI, MCP server ... witness-signed release provenance'); code search confirmed 'Darwin Mode', '@metaharness/darwin', 'Cost-Pareto', 'SWE-bench Lite', 'LiveCodeBench' and 'strictly disjoint' train/eval all present in-repo; fetched SUBMISSIONS.md directly and confirmed the exact phrase 'Wilson 95% CI' gating leaderboard submissions.\n10. All ten entries' external references in this range are markdown hyperlinks (rule 7) — no bare repo/tool citations found. No absolute local paths, credentials, or reverse-engineered internals originating from this KB (rule 8) were found; the one source that does contain reverse-engineered internals (Harness Books) is a legitimately-cited third party, not this repo's own binary-grep.

### `34[171-344]` — docs/34-loop-patterns.md (Part I)

170 previously-unverified lines · 14 claims checked · 8 URLs opened · 7 findings · **8.2 claims per 100 unverified lines**

Verified against source repos (all confirmed to exist, all default_branch=main, all citations properly markdown-linked per rule 7): (1) the 'CI Sweeper > PR Babysitter > Dependency Sweeper > Post-Merge Cleanup > Daily Triage' priority order and the 'one owner per branch per hour' rule — exact match to cobusgreyling/loop-engineering docs/multi-loop.md; (2) the Three-Loop Onboarding Sequence's loop selection and ordering (Daily Triage, PR Babysitter, Post-Merge Cleanup) — matches that same source's 'Example: safe three-loop setup' table; (3) Docs Sync Loop's action description (drift detection: CLI flags/commands/options renamed or removed, stale examples, undocumented behavior) and its L2/PR-not-automerge readiness and worktree-isolation scope — match faisalishfaq2005/loopflow's templates/docs-sync.yaml; (4) the literal string 'VERDICT: PASS' as the loopflow gate's pass signal — confirmed in that repo's own README screenshot caption; (5) v2.1.243's `/usage` Loops breakdown claim (lines 178-180, newer content) — confirmed verbatim against the real anthropics/claude-code CHANGELOG.md entry for 2.1.243, and the cost-control.md anchor it links to exists; (6) the Knowledge-Base Tracker Loop's '05:00 local' cron claim (line 327) — confirmed against this repo's own scripts/com.luca.loop-news.plist and SCHEDULING.md; (7) both this-repo self-citation paths at line 344 (.claude/skills/, scripts/run-loop-news.sh) resolve to real paths in the current tree; (8) all five external repo/tool citations in this range are markdown hyperlinks to the correct canonical URLs, no bare-slug violations found.

### `07[1-230]` — docs/07-subagents.md (Part II)

167 previously-unverified lines · 17 claims checked · 14 URLs opened · 4 findings · **10.2 claims per 100 unverified lines**

Verified as accurate and left unchanged: (1) the Sabrina Ramonov quote and attribution at lines 44-46, cross-checked verbatim against https://www.sabrina.dev/p/loop-engineering-claude-code-goal-routines (datePublished 2026-06-19T19:25:29Z, confirming 'Jun 2026'). (2) The orobsonn/claude-harness dead-source disclosures at lines 84 and 94 — independently confirmed the repo and its v0.20.0 release tag both 404 (gh api + curl, checked 2026-09-07) and that no Wayback snapshot exists for either URL (archive.org availability API returned empty for both), so the existing 'kept, dated, flagged rather than deleted' treatment is fully compliant with the KB's own rule and needed no change. (3) All three version-stamped changelog quotes in the 'Running in the background' table (v2.1.198, v2.1.259, v2.1.260, lines 179-181) and the v2.1.246 'Fixed' quote at line 154 — all four are character-for-character exact matches against https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md. (4) The wquguru/harness-books 'task forwarding' / synthesis claims (lines 111-133) — confirmed via authenticated gh api search/code and by pulling chapter-07-multi-agent-and-verification.md directly; the KB's paraphrase tracks the chapter's own 'Without this layer, multi-agent degrades into polite task forwarding' and its delegable/non-delegable framing closely. (5) The Kanevry/session-orchestrator confidence-gate claims (lines 134-150), including the specific '≥80%' threshold — confirmed exactly against agents/session-reviewer.md lines 122-130 ('Only include findings with confidence >= 80 in the main section reports'). (6) The built-in subagent types table (lines 199-212) and the Explore footnote (lines 213-222, dated v2.1.198) — cross-checked every model/tools claim (fork, general-purpose, Explore, Plan) against the live https://code.claude.com/docs/en/sub-agents; all rows are accurate simplifications of current behavior, and the Explore footnote's quoted text is an exact match. (7) All internal cross-doc anchor links used in this range (04-verification.md#verifier-integrity-keeping-the-check-unfakeable, 04-verification.md#loop-verdict-taxonomy, 37-session-architecture.md#the-finding-that-actually-mattered, 39-dynamic-workflows.md#the-script-api, 11-cost-control.md, #nesting) resolve to real headers in the target files.

### `32[1-180]` — docs/32-reading-list.md (Part II)

162 previously-unverified lines · 28 claims checked · 11 URLs opened · 4 findings · **17.3 claims per 100 unverified lines**

Checked and found accurate: (1) all 8 external URLs in range resolve live (Osmani, TheNewStack, roanbrasil/Medium via search, Ronacher, Sabrina Ramonov, Data Science Dojo, Lenny's/Claire Vo, Andrew Ng/The Batch, Ben's Bites — 9 total including the already-verified TheNewStack piece); (2) the roanbrasil entry's 0.9^10=35% math and 'mediocre model...frontier model invoked once' quote, and its @roanbrasil X-handle attribution; (3) Ronacher's 'amplification effect' (confirmed as 'amplify... each iteration adds another small defense') and 'cognitive dependency' paraphrases, and the article's publish date (2026-06-23) and title; (4) Sabrina Ramonov's DOER/CHECKER framing, the verbatim stopping-condition quote 'If you can't say what \"done\" looks like, you don't have a loop. You have a wish', and the article's publish date (2026-06-19); (5) Data Science Dojo's ~4x/~15x token-cost stat (verbatim match) and the Inner/Outer Dual Loop description (correctly Generation-3/2024, attributed in source to Microsoft's Magentic-One, which the KB doesn't misstate); (6) Andrew Ng/The Batch entry — 'context advantage' phrase, the three-loop cadence (agentic coding = minutes, developer feedback = 'tens of minutes and hours', external feedback = days) all verbatim-matched, and its title/URL (already corrected 2026-09-04, re-verified live); (7) Claire Vo/Lenny's Newsletter entry — host attribution, job-description/onboarding framing, and heartbeat/cron/hook/goal-loop coverage all verbatim-matched; (8) Ben's Bites entry — turn-based/goal/loop/schedule/routines list verbatim-matched, publish date (2026-07-07), and its cross-reference anchor docs/24#the-official-claude-team-loop-types (confirmed the heading '### The Official Claude Team Loop Types' exists at docs/24-harness-patterns.md:489); (9) all citations in range are already markdown hyperlinks (rule 7) — no bare-slug or bare-name violations found; (10) docs/32 is a curated external-reading-list doc, not a platform-facts doc, so I did not apply the Part II version-stamp requirement to article summaries/dates here — the doc topic map itself labels doc 32 'Reading list — curated external articles', distinct from the flag/version/limit docs the version-stamp promise targets, and none of its sentences assert a current Claude Code capability as fact outside of quoting what an article covers.

### `33[1-190]` — docs/33-agent-security-hardening.md (Part II)

162 previously-unverified lines · 14 claims checked · 9 URLs opened · 3 findings · **8.6 claims per 100 unverified lines**

Checked and found CORRECT (with source): (1) OS-User-Per-Agent Isolation + Credential Isolation (lines 10-45) — clem's docs/threat-model.md confirms the exact 'four dispositions' (broker/sidecar/remove/egress-firewall) framing, the no-plaintext-secret guarantee, and per-UID nftables egress containment described. (2) SECURITY_MATRIX.md section (47-59) — the file exists verbatim at github.com/jahwag/clem/blob/main/SECURITY_MATRIX.md; the `kill $PPID` known-safe example and known-attack-pattern framing are accurate paraphrases of the real file. (3) Runtime Policy Gating table (61-76) — omnigent-ai/omnigent's blast_radius policy (omnigent/policies/builtins/orchestration.py) matches the described ASK/DENY-by-command-class mechanism; intent_gate/blast_radius both verified present in the live repo. (4) Where Default-Deny Gets Loaded (85-105) — all four cited repos/paper resolve and match their described role: mcpharbour (now redirects to steerholm/mcpharbour but the link still resolves — repo renamed, not dead), saagpatel/cross-provider-egress-guard, ActPlane arXiv 2606.25189 ('ActPlane: Programmable OS-Level Policy Enforcement for Agent Harnesses' — title confirmed live), codeafix/agent-assistant — descriptions match the doc's claims about each layer. (5) v2.1.232 and v2.1.260 changelog quotes (107-133, already flagged as post-2026-09-04 content and lower priority) — re-verified verbatim against the live CHANGELOG.md as a sanity check; both exact. (6) Credential Rotation Mid-Session (134-158) — rashmi1112/Credential-Sentinel's backend/app/graph/{nodes.py,state.py} confirm the exact DEFER/OWN_STALE/OWN_UNMANAGED/UNKNOWN reconciliation states, the discover→reconcile→assess→prioritize→plan pipeline stage names, the two-gate structure, and the promote→repoint→verify cutover with revert-on-failure. (7) Session Watchdog (173-189) — jahwag/clem's internal/runner/runner.go confirms `timeout 7200` (exactly 2 hours) as the hard per-session limit matching the doc's 'e.g. 2 hours' example, and internal/watchdog/watchdog.go confirms a systemd-service-based restart-on-crash mechanism matching the described watchdog behavior. All 8 external repo/paper links in range resolve on their default branch (`main` in every case checked).

### `32[361-520]` — docs/32-reading-list.md (Part II)

145 previously-unverified lines · 36 claims checked · 24 URLs opened · 9 findings · **24.8 claims per 100 unverified lines**

Checked and found accurate: (1) the Gusto/Lenny's Newsletter article — 'five-person team', 'ten weeks' (10 weeks), 'tier-one AI product', and the 'trash-can method' term are all directly confirmed in the cited page's show notes. (2) The Claude Tag entry's Governance claims other than the sandbox clause — token caps per org/channel, audit logging (log of everything @Claude has done + who requested it), and channel-scoped/role-based memory separation — are all supported in paraphrase by the cited Anthropic page. The 65% statistic is accurate in substance (only the adjacent quoted sentence was mis-transcribed, flagged separately). (3) goal-engineering repo's G0–G3 readiness-scoring tool (goal-audit) and goal-cost token-spend estimator are both real, confirmed in the current README. (4) fleet-engineering's F0–F3 maturity gates (Fleet Ready Score), the Assistant-vs-Claw identity-model link, and fleet-budget.md as a template artifact are all confirmed. (5) session-orchestrator: the five-wave architecture (Discovery→Impl-Core→Impl-Polish→Quality→Finalization), STATE.md crash-recovery-by-wave, the ≥80%/confidence-≥80 review-gate threshold, and Wave 4 being a simplification pass run before test authoring are all confirmed verbatim or near-verbatim in the current README. (6) loop-contract-skill: fold_ledger.py as the gate script, the DONE-vs-KNOWN mode distinction, and the exact tagline quote 'an agent is bounded by its verifier, not its model' are all confirmed against the repo's own description and README. (7) Cross-references check out: the loop-contract-skill entry's claim of replacing loop-kernel is consistent with docs/27-loop-contract.md's actual citation of uppifyagency/loop-kernel at its '#reference-implementation-the-three-exit-codes' anchor (verified the anchor resolves); the LoopX entry's claim of replacing Strive_Engineering is consistent with docs/04-verification.md's citation of krishddd/Strive_Engineering at its '#verifier-integrity-keeping-the-check-unfakeable' anchor (verified the anchor resolves). (8) LoopX's `claimed_by` todo-ownership claim is confirmed in the README. (9) Citation-link hygiene: no bare owner/repo slugs or unlinked repo/tool references found anywhere in lines 361-520. (10) All 7 cited URLs (3 articles, 4 GitHub repos) return HTTP 200 / resolve via gh api, and default_branch=main was confirmed for all four cited repos before any raw-content fetch.

### `12[1-160]` — docs/12-hooks.md (Part II)

144 previously-unverified lines · 32 claims checked · 27 URLs opened · 7 findings · **22.2 claims per 100 unverified lines**

Checked and found CORRECT: (1) all 5 hook-type names and one-line descriptions (command/http/mcp_tool/prompt/agent) exist as documented; (2) `agent` hook type's '(experimental)' label and 30s/60s timeout defaults for prompt/agent respectively; (3) 13 of the 16 rows in the 'Key lifecycle events' Can-block table (lines 66-83) — SessionStart, Setup, UserPromptSubmit, PreToolUse, PostToolUse, PostToolBatch, SubagentStart, SubagentStop, Stop, StopFailure, PostCompact, PreModelSwitch, PostModelSwitch, SessionEnd all match current docs (only PermissionRequest and PreCompact are wrong, reported above); (4) the v2.1.251 PreModelSwitch/PostModelSwitch and SessionStart-resume-hook claims (lines 81-96) match the changelog nearly verbatim; (5) the '8 consecutive blocks' numeric value itself (still current, only missing a stamp — see finding); (6) JSON output API field names and semantics for continue/suppressOutput/systemMessage/hookSpecificOutput/updatedInput/updatedToolOutput/additionalContext (lines 102-127), and the `permissionDecision` allow|deny|ask|defer value set specifically for PreToolUse; (7) `async`/`asyncRewake` field behavior (lines 131-156) — asyncRewake correctly described as waking Claude on exit code 2; (8) the Kanevry/session-orchestrator repo exists and is a real, actively maintained public repo (only its specific attribution to the bash deny-hook pattern is wrong, reported above); (9) the citation at line 62 is already a markdown hyperlink (satisfies the citation-must-link rule).

### `34[1-170]` — docs/34-loop-patterns.md (Part I)

140 previously-unverified lines · 14 claims checked · 9 URLs opened · 1 findings · **10.0 claims per 100 unverified lines**

Checked and found CORRECT, well-sourced, and already properly hyperlinked (rule 7 compliant — no bare repo/tool names found in this range): (1) the Cobus Greyling / cobusgreyling/loop-engineering attribution (line 7) — repo exists, created 2026-06-09 (\"Jun 2026\" matches), and its README pattern table matches the KB's seven named patterns (Daily Triage, PR Babysitter, CI Sweeper, Dependency Sweeper, Post-Merge Cleanup, Changelog Drafter, Issue Triage) almost exactly. (2) The internal link to docs/20-loop-maturity-model.md (line 11) and its \"14-step\" developer maturity claim — the linked doc does describe a 14-step community roadmap and does state a step-14 engineer still starts every new loop in report-only/L1 mode, consistent with this doc's line 13. (3) The v2.1.248 changelog quote (lines 32-40) — verified verbatim against the live GitHub release body: \"Changed `/loop`: self-paced dynamic mode and the no-prompt autonomous default are now always available, including on Bedrock/Vertex/Foundry.\" Exact match. Also cross-checked the \"before that release\" platform-restriction history claim against the full CHANGELOG.md: v2.1.73 made /loop available at all on Bedrock/Vertex/Foundry, and only v2.1.248 later removed the finer self-paced/no-prompt-default restriction — consistent with the KB's historical framing. (4) The v2.1.243 changelog quote (lines 42-48) — the GitHub Releases API 404s for this tag (as CLAUDE.md's own KB_GAPS pattern predicts: git tag exists, no GitHub Release object), but the release *page* itself returns HTTP 200 (link is not dead for readers), and CHANGELOG.md at the v2.1.243 tag confirms the quote verbatim: \"Improved `/loop`: consecutive wake-ups where Claude has nothing to do now fold into a single line in the terminal instead of printing each one.\" (5) The DEPBENCH / arXiv 2608.30300 claim (lines 114-122) — paper exists at arxiv.org/abs/2608.30300, titled \"Update from Hell: Can Coding Agents Survive Hidden Breakage in Dependency Upgrades?\", submitted 31 Aug 2026 (matches \"Aug 2026\"), abstract confirms 203 real-world tasks, 5 package ecosystems, and the best configuration solving 104/203 (51.2%) — all figures in the KB match exactly.

### `05` — docs/05-claude-md.md (Part II)

125 previously-unverified lines · 17 claims checked · 8 URLs opened · 7 findings · **13.6 claims per 100 unverified lines**

Confirmed CORRECT against current docs (code.claude.com/docs/en/memory) and, where the KB text predates 2026-09-04, cross-checked against the closest Wayback snapshot to the line's own commit date: (1) all four concrete CLAUDE.md load-hierarchy paths — managed policy macOS path, ~/.claude/CLAUDE.md, ./CLAUDE.md or ./.claude/CLAUDE.md, ./CLAUDE.local.md (lines 64-67); (2) subdirectory CLAUDE.md files loading lazily on file-read (line 68) — matches 'included when Claude reads files in those subdirectories'; (3) the path-scoped rules YAML frontmatter format and glob-pattern example (lines 76-90) — matches the documented `paths:` key and glob-matching behavior, including that matches trigger on file-read, not every tool use; (4) 'Maximum 4 import hops' (line 103, first clause) — matches current docs' 'maximum depth of four hops' verbatim in substance; (5) the 'What belongs where' table (lines 40-49), all 5 rows, against current guidance on CLAUDE.md vs skills vs CLAUDE.local.md vs subdirectory files; (6) the claudeMdExcludes JSON key name and array-of-glob-patterns syntax (lines 119-123) — correct, only the recommended file location differs (see finding); (7) the Addy Osmani citation added 2026-09-05 (line 38) — URL resolves (200), article headline 'Audit your Agent files' by Addy Osmani, published 2026-08-27, matching the KB's 'Aug 2026' attribution exactly; the KB does not present this as a verbatim quote, just an attributed reference, so no misquotation risk. Also confirmed the doc's opening framing ('short and surgical' / 'bloated CLAUDE.md files cause Claude to ignore instructions', lines 6-9) is directionally consistent with current official guidance ('target under 200 lines... longer files consume more context and reduce adherence').

### `25` — docs/25-long-running-agents.md (Part I)

122 previously-unverified lines · 11 claims checked · 11 URLs opened · 4 findings · **9.0 claims per 100 unverified lines**

Checked and found correct: (1) `claude --bg`/`--background` and `claude agents` (lines 142) match the current official CLI reference (code.claude.com/docs/en/cli-reference) verbatim. (2) The jahwag/clem repo cited at line 169 exists, is a real Claude Code multi-agent fleet tool, and its README's own description ('systemd + tmux per agent. Watchdog timer restarts dead or stalled sessions') supports the general watchdog/systemd pattern the doc describes (the specific '2 hours' figure at line 157 is presented as the KB's own illustrative example, not attributed to clem, so it needs no source). (3) Citation-link hygiene across the full 176-line range: grepped for bare `github.com` or `@handle` mentions not wrapped in a markdown link — none found; every external reference in this unit is already a hyperlink (rule 7 clean). (4) The raw statistics in the samwillis passage (1,000 commits / 124,000 lines / 786 files / 10 days) are numerically accurate and match the primary-source tweet exactly — only the surrounding characterization is wrong (see finding). (5) Confirmed via docs/30-goal-engineering.md that Claude Code does ship its own native `/goal` command (since v2.1.139, per that doc), which is a separate, uncredited fact-check surface not itself contradicted here.

### `12[161-304]` — docs/12-hooks.md (Part II)

119 previously-unverified lines · 22 claims checked · 8 URLs opened · 5 findings · **18.5 claims per 100 unverified lines**

Checked and found CORRECT (content, not just presence of a version stamp): the asyncRewake verify-output.sh script's use of exit-code-2 + hookSpecificOutput.additionalContext (lines 158-169, matches documented asyncRewake behavior); the `if` field's pattern-syntax equivalence to permissions.allow/permissions.deny (171-186, both keys confirmed to exist in the permissions docs); every row's *content* in the environment-variables table (192-197: CLAUDE_PROJECT_DIR description, CLAUDE_EFFORT's exact five-value list low/medium/high/xhigh/max, CLAUDE_CODE_REMOTE's cloud-session claim including the Routines parenthetical, CLAUDE_CODE_REMOTE_SESSION_ID description, CLAUDE_ENV_FILE description) — all match the live env-vars reference; the already-reviewed warning box's two verbatim quotes (203-205, 207-211) re-verified word-for-word against the live docs; the 'Gate a model switch (v2.1.251+)' claim (285-292) — changelog confirms PreModelSwitch/PostModelSwitch were added exactly at v2.1.251; the 'Related' section's four internal links (299-304) — all four target files exist, and 17-failure-patterns.md does contain a 'Missing circuit breaker' entry matching the cross-reference's description.

### `07[231-464]` — docs/07-subagents.md (Part II)

107 previously-unverified lines · 25 claims checked · 9 URLs opened · 3 findings · **23.4 claims per 100 unverified lines**

Confirmed correct against live official docs (code.claude.com/docs/en/sub-agents), the Claude Code CHANGELOG.md, and GitHub (gh api, dated commits): (1) frontmatter fields in the custom-agent example (model: claude-sonnet-5 — confirmed as a valid full-model-ID example verbatim in the docs' 'Choose a model' section; permissionMode: auto — confirmed a valid enum value; tools: list format); (2) the 5-tier agent-file loading priority order (managed settings → --agents → .claude/agents/ → ~/.claude/agents/ → plugin agents/) — matches the docs' priority table exactly; (3) the v2.1.198 /agents-wizard-removal blockquote — verbatim match; (4) permission-rule syntax: Agent(model:opus), Agent(Explore), deny:[\"Agent\"], CLAUDE_AGENT_SDK_DISABLE_BUILTIN_AGENTS=1 — all confirmed against the live permissions reference page, syntax and semantics match exactly; (5) version-history claims: v2.1.212 (200-spawn cap added), v2.1.224 (cap removed, quote verbatim-matches CHANGELOG), v2.1.217 (depth default →1, quote verbatim; also concurrent-subagent-cap introduction, default 20), v2.1.219 (depth default →3, quote verbatim), v2.1.251 (CLAUDE_CODE_SUBAGENT_MODEL demoted to a default, quote paraphrase matches CHANGELOG's 'Changed CLAUDE_CODE_SUBAGENT_MODEL to set the default subagent model rather than override everything' entry), v2.1.257 (CLAUDE_CODE_SUBAGENT_MODEL_FORCE added, matches CHANGELOG), v2.1.243 (/tasks model+effort display, matches CHANGELOG entry verbatim: 'Added the model (and effort level) each subagent ran on to /tasks and the agent detail dialogs') — 8 distinct version claims, all confirmed; (6) fork mechanics — 'subagent_type: \"fork\"' terminology and context/cache inheritance confirmed against the live docs' Fork vs non-fork comparison table; (7) the Spec-stage (9 checks) and Implementation-stage (9 checks) reviewer checklists and the 'Rationalizations we refuse' table, cited to eugenelim/agent-ready-repo (Jun 2026) — fetched the repo's file at commit a2bd23d0 (2026-06-12, the version contemporaneous with the KB's capture date) and confirmed the KB's 18 checklist items and 3 rationalization rows match that dated snapshot closely (paraphrased but faithful); the repo has SINCE (Aug 2026) restructured this file and removed 5 of those spec-stage checks in a later refactor, but per Rule 2 this does not falsify the KB, which is dated 'Jun 2026' and was accurate as of that capture — no fix needed. Also confirmed: harness-books.agentway.dev citation URL resolves (200); maskshell/solidforge repo exists, is public, and its description corroborates the 'citation verification' / 'prior-art collision search' claim attributed to it (default_branch main, checked directly, not assumed).

### `28` — docs/28-routines.md (Part II)

105 previously-unverified lines · 13 claims checked · 5 URLs opened · 5 findings · **12.4 claims per 100 unverified lines**

Checked and found accurate (both against current docs and, where noted, against a Wayback Machine snapshot dated before this doc's own authoring dates, so these were also correct at capture time): (1) the comparison table's 'Local filesystem/credentials' and 'Trigger types' rows (lines 9-17); (2) the 'minimum interval: 1 hour' schedule claim (line 27); (3) the `/v1/claude_code/routines/<routine-id>/fire` endpoint path, `Authorization: Bearer` header, and the `anthropic-beta: experimental-cc-routine-2026-04-01` beta header value (lines 46-48) — this section already carries an explicit in-doc hedge to verify against official docs before production use, which the content review confirms was and is warranted only for the fields that are missing (host, anthropic-version, Content-Type), not for what's shown; (4) the `/schedule list` / `/schedule update` / `/schedule run` CLI commands (lines 35-37), which match the current docs verbatim; (5) the general 'cloud execution primitive... runs on Anthropic infrastructure... laptop can be closed' framing (lines 3-5), true at capture date (self-hosted-environment routing was added to the docs later, which doesn't falsify the original text, only makes it incomplete — not flagged given no version marker exists to attach that addition to and it doesn't contradict what's there). Lines 90-172 (the 2026-09-04+ content: production examples, provenance note, v2.1.251 MCP note) were left largely unre-litigated per the task's framing that an earlier pass already verified content from that date forward; I did not find contradicting evidence in the course of this pass that would reopen them.

### `01` — docs/01-paradigm-shift.md (Part I)

103 previously-unverified lines · 14 claims checked · 9 URLs opened · 6 findings · **13.6 claims per 100 unverified lines**

Checked and found CORRECT: (1) the Andrew Ng quote at lines 9-10 ('most effective coding agent use is a complex, highly iterative process') — verbatim match against a live curl of the DeepLearning.AI Batch article, correctly dated 2026-09-04. (2) The Sabrina Ramonov 'AI Leverage = Your Skill × Your Clarity' formula and the Clarity definition at lines 35-49 — verbatim match against sabrina.dev, correctly dated Jun 2026 (article datePublished 2026-06-19). (3) The compound-probability math at lines 53-56 — 0.9^10 ≈ 0.3487, correctly stated as ≈35%. (4) The five-pattern table (Prompt chaining / Routing / Parallelization / Orchestrator-workers / Evaluator-optimizer) at lines 96-102 and its Dec 2024 citation at line 106 — all five pattern names and descriptions verified present in Anthropic's live 'Building Effective Agents' post, correctly dated Dec 19, 2024. (5) The Addy Osmani article's title and Jun 2026 date at line 88 — page <title> is verbatim 'The New Software Lifecycle', datePublished 2026-06-16. (6) The substantive claim at lines 110-119 (~400,000 Claude Code sessions; domain expertise, not coding background, predicts work-per-instruction) — confirmed against the live Anthropic Research page content (only its citation title/date were wrong, filed as a separate finding). (7) All four internal doc links in this unit (14-human-in-the-loop.md, 26-factory-model.md, 35-choosing-your-mode.md, 07-subagents.md) resolve to existing files. (8) No bare repo/owner slugs or un-hyperlinked repo references found in this doc.

### `33[191-387]` — docs/33-agent-security-hardening.md (Part II)

100 previously-unverified lines · 22 claims checked · 19 URLs opened · 5 findings · **22.0 claims per 100 unverified lines**

Checked and found CORRECT (all pre-2026-09-04 lines unless noted): (1) 'Relationship to Permissions & Allowlists' (lines 209-219) — the described contents of 08-permissions.md (risk-tiered authorization by consequence, allow/deny/ask lists, safety path denylist) all confirmed present in that file by direct grep. (2) credbroker's `pip install credbroker` command and its citation link to pypi.org/project/credbroker/ (line 227-231) — package is real, install command correct, PyPI page live (200). eugenelim/agent-ready-repo citation (Jun/Jul 2026, lines 243 and 259) — repo real, both citation dates plausible against repo commit history. (3) 'Grading Harness Security Posture' section (lines 261-274) — saagpatel/harness-scorecard repo verified real; ten graded dimensions, capability-gate mechanism, the specific bypassPermissions/hard_deny example, and the vulnerable/guarded fixture-pair testing methodology all confirmed verbatim against the repo's README and docs/rubric.md. (4) 'Cross-Org Federation' section (lines 276-299) — ruvnet/ruflo repo verified real; mTLS + ed25519 identity/signing (including 'signing ... on every send', not just handshake), the exact trust-scoring formula (0.4×success + 0.2×uptime + 0.2×threat + 0.2×integrity), and the 14-type PII detection pipeline with BLOCK/REDACT/HASH/PASS policies all confirmed verbatim in the repo's README and docs/federation/README.md. (5) 'Hook and Context Trust Attacks' section (lines 301-345) — all three arXiv papers (2609.03884, 2609.01222, 2608.27299) confirmed real and live; every quoted statistic (HookPry: all 7 harnesses, 1,000 runs, 92.5% per-harness success, 0% Defender recall; CPE paper: 12 harnesses including Claude Code and Codex; instruction-privilege-escalation paper: 13 objectives × 6 harnesses, all 13 on all 6, and all 13 on the 3 harnesses with automatic permission review) matches the fetched abstracts essentially verbatim. (6) 'Emergent Multi-Agent Coordination Risk' rogue-wiki figures (lines 346-368) — Simon Willison's post and the explainx.ai DseWiki post both live (200); '~18,000 posts' matches the explainx.ai article's own figure; 'exchange thousands of messages' consistent with Willison's reported ~13,000 edits in one week. (7) 'Relationship to MCP Security' (lines 380-387) — confirmed docs/19-mcp-security.md covers AgentJacking/indirect prompt injection and docs/23-fleet-engineering.md exists, matching the cross-references. (8) Grepped the full 191-387 range for bare `owner/repo` GitHub slugs outside markdown link brackets — none found (all repo citations in the range are properly linked except the two product-name gaps reported above).

### `26` — docs/26-factory-model.md (Part I)

97 previously-unverified lines · 23 claims checked · 15 URLs opened · 1 findings · **23.7 claims per 100 unverified lines**

Verified accurate and well-sourced, with the source fetched and checked directly: (1) Boris Cherny quote 'My job is to write loops' (line 29) — confirmed verbatim via yt-dlp auto-captions of youtube.com/watch?v=RkQQ7WEor7w at ~11:52 (linked timestamp 11:45 is a reasonable ~7s lead-in); video title/channel/upload-date (2026-06-02, WorkOS, 'Boris Cherny: Claude Code & the Future of Engineering | Acquired Unplugged presented by WorkOS') match the KB's speaker/event/date attribution exactly. (2) Both Andrew Ng quotes (lines 46-50, 58-59) — confirmed verbatim against the live deeplearning.ai article; its datePublished (2026-07-10T07:30:42-07:00) matches the KB's cited date exactly. (3) MindStudio 'dark factory' 5-stage pipeline claim (lines 81-94) — the live article's actual Stage 1-5 headers (Spec Ingestion and Decomposition / Code Generation / Automated Testing and Validation / Code Review (Automated) / Version Control and CI/CD Integration) and its 'Spec Ambiguity... garbage in, garbage out' failure-mode text substantively match the KB's 'spec → planning agent → code-gen agents → testing → review agent → deployment' gloss and underspecified-input bottleneck claim; datePublished 2026-07-04 matches 'Jul 2026'. (4) OpenAI 99%-of-tokens and 99th-percentile 60+-hours claims (lines 98-99) — both confirmed verbatim in the live article text (fetched via jina.ai proxy after direct fetch was Cloudflare-blocked). (5) Bun rewrite figures (lines 138-148) — 535,496-line count, ~16,000 compiler errors, and the '64 claudes at once' (4 worktrees × 16 Claudes) figure, plus the PORTING.md/LIFETIMES.tsv porting-guide workflow, all confirmed verbatim in the live bun.com article; datePublished 2026-07-08 matches 'Jul 2026'. (6) Thorsten Ball / Nate Berkopec auto-merge discussion (lines 131-137) — both tweets fetched via oembed; content ('very little a human can do better in a review than a GPT-5.6'; 'a factory that merges these within 5 minutes of them being opened') substantively matches the KB's paraphrase, including the 'within minutes' framing. (7) All three internal cross-references at the end of the doc (docs/27, docs/24, docs/04) and the two anchor links (01-paradigm-shift.md#the-new-software-lifecycle, 23-fleet-engineering.md#case-study-bun-64-parallel-instances-rewriting-535k-lines-in-11-days) resolve to real headers. (8) The cross-reference to 17-failure-patterns.md's 'Dark factory' table entry (line 91) accurately describes that entry's content. (9) Lines 1-40 and 174-178 (the 2026-06-22 original, pre-dating the 2026-09-04 cutoff) contain no external, checkable factual claims — general descriptive prose about the factory-model concept with no version numbers, prices, named-tool claims, or absence assertions.

### `09[1-140]` — docs/09-headless-mode.md (Part II)

95 previously-unverified lines · 23 claims checked · 6 URLs opened · 4 findings · **24.2 claims per 100 unverified lines**

Checked as accurate against the current official CLI reference (code.claude.com/docs/en/cli-reference), the headless-mode docs page, model-config.md, permission-modes.md, and the full CHANGELOG.md: --output-format (json/stream-json), --max-turns, --max-budget-usd, --permission-mode auto, --allowedTools syntax, --continue/--resume/--fork behavior, --system-prompt/--system-prompt-file, --append-system-prompt/--append-system-prompt-file, --append-subagent-system-prompt (and its v2.1.205+ stamp, exact match), --model claude-opus-5 (confirmed as the documented full model name), --effort high, --fallback-model claude-sonnet-5,claude-haiku-4-5 (comma-separated syntax and exact model IDs both confirmed against the official fallbackModel example), --bg and the claude agents/attach/logs commands, the --exclude-dynamic-system-prompt-sections behavior description (mechanism matches, only the version stamp was missing), --no-session-persistence's behavior description, --safe-mode's behavior description (matches official help almost verbatim), --bare's behavior description (matches the official headless.md page point-for-point, including the --add-dir skills partial exception), the v2.1.259+ section header and its verbatim quoted release note (exact match, and the GitHub release itself resolves, not a 404), the --permission-prompts-none vs --dangerously-skip-permissions contrast table, the entire \"print-only guarantee\" warning box (the --bg/--print conflict, the exact \"--help\" quotes for --permission-prompts and --max-budget-usd, both character-for-character matches), and the JSON/stream-json structured-output examples. No bare/unlinked external references (repo, tool, product, @handle) found in this line range — rule 7 is clean here. No absence-of-X claims found in range.

### `13` — docs/13-context-management.md (Part II)

88 previously-unverified lines · 21 claims checked · 18 URLs opened · 2 findings · **23.9 claims per 100 unverified lines**

Checked and found CORRECT: (1) The entire 'New Rules for Claude 5-Generation Models' section (lines 5-33) — the 'over 80%' figure, the three Then/Now table rows, both verbatim system-prompt quotes (old comments rule and its replacement), and the Jul 2026 publish date, all verified against a live fetch and a Wayback snapshot of the Anthropic blog post. (2) The 'Auto-compact thresholds by model' table and all three verbatim release-note quotes (v2.1.247's 967K/934K figures, v2.1.260's Opus/Fable wording, v2.1.235's context-limit-error wording) — confirmed byte-for-byte against `gh api repos/anthropics/claude-code/releases/tags/<tag>`. (3) The Reactive Compact section's three specific numbers — 20,000-token output reserve, 13,000-token early-warning buffer, and the 3-consecutive-failures circuit breaker — all confirmed against wquguru/harness-books (MAX_OUTPUT_TOKENS_FOR_SUMMARY=20_000, AUTOCOMPACT_BUFFER_TOKENS=13_000 in chapter-05; MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES=3 in chapter-06). (4) The 'Clean the site first, then execute' quote and the Input Governance Pipeline's five-step order (prefetch/snip/microcompact/collapse/autocompact), both matched against chapter-03. (5) The `/compact Focus on...` custom-instruction example syntax, confirmed against code.claude.com/docs/en/costs, which shows the identical pattern (`/compact Focus on code samples and API usage`). (6) wquguru/harness-books' default branch is `main` (checked before raw-fetching, per rule 4) and the repo genuinely exists with the claimed AgentWay branding. (7) All three internal doc links/anchors referenced in this range (05-claude-md.md, 27-loop-contract.md, 21-context-vs-loop-engineering.md#a-second-debate-loops-vs-graphs-jul-2026) resolve to real files/headings. (8) All external references in this range are already markdown hyperlinks — no bare-slug or bare-name citations found (rule 7 clean).

### `14` — docs/14-human-in-the-loop.md (Part I)

79 previously-unverified lines · 21 claims checked · 18 URLs opened · 4 findings · **26.6 claims per 100 unverified lines**

Checked and found CORRECT: (1) all four MindStudio checkpoint-blog quotes/claims — the verbatim 95%-override-rate quote (lines 37-38), the 80/20 split claim (32-33), the 'two to three checkpoints' claim, and the four-test table content (irreversibility/confidence/external-visibility/context-gap), plus its Jun 2026 publish date (article's own datePublished/dateModified: 2026-06-30). (2) Both Andrew Ng verbatim quotes (lines 117-118, 123-124) match the source exactly, and the article's 2026-06-26 publish date (line 136) matches its own schema. (3) Cross-reference anchors to docs/25-long-running-agents.md#innerouter-dual-loop and docs/17-failure-patterns.md (Silent Default Drift row) both resolve. (4) In the newer (2026-09-04+) 'Checkpoint Has a Timeout' section: the CHANGELOG.md 2.1.200 quote (exact match), the env-vars-reference 2.1.198/2.1.199 60000ms quote (exact match, confirmed live on code.claude.com/docs/en/env-vars), the olafalders.com article's existence and its 'pins it to 2.1.198 by diffing binaries' claim, the HN thread's 142-points/120-comments count (exact match via HN Algolia API), the Anthropic engineer's verbatim quote 'the rollout should have been opt-in (like it is now) and on the Changelog' (exact match, from user trq_/Thariq, confirmed as a real Claude Code team member's in-thread reply), and the practitioner's verbatim 60-second-window quote (exact match, from user Tadpole9181). (5) The classifier-fallback behavior described at lines 16-18 is substantively real and current (just imprecisely worded/uncited — see findings). No rule-7 bare-slug/uncited-repo violations found anywhere in lines 1-160.

### `08[1-250]` — docs/08-permissions.md (Part II)

67 previously-unverified lines · 19 claims checked · 4 URLs opened · 5 findings · **28.4 claims per 100 unverified lines**

Verified clean (old, pre-2026-09-04 lines, per git blame): (1) lines 1-2 title; (2) lines 12-25 '## 1. Permission allowlists' — the settings.json allow-list syntax (Bash(npm run *), Bash(git *), Bash(pnpm *), bare Edit/Read) matches current docs exactly; (3) line 33 `claude --permission-mode auto -p "..."` — confirmed the --permission-mode flag is documented to work with -p for non-interactive runs; (4) line 213 `claude --permission-mode bypassPermissions -p "run migration"` — same, valid combination, and --dangerously-skip-permissions is confirmed equivalent; (5) lines 218-229 the allow/deny/ask JSON example and the deny/ask one-line descriptions (deny=blocked outright, ask=prompts even in auto mode) — both individually confirmed correct against current docs' 'Manage permissions' section. Also fully re-verified (spot-checked, though this is newer content already covered by a prior pass) every verbatim CHANGELOG.md quote and every version number in the 'Auto mode: what changed' table (lines 97-113: v2.1.205 x2, v2.1.207 x2, v2.1.208, v2.1.210, v2.1.211, v2.1.214, v2.1.236 x2, v2.1.246, v2.1.248, v2.1.257 x2) against the live raw CHANGELOG.md — every quote is character-exact and every version number matches the changelog header the quote actually appears under; no drift found.

### `21` — docs/21-context-vs-loop-engineering.md (Part I)

67 previously-unverified lines · 6 claims checked · 5 URLs opened · 2 findings · **9.0 claims per 100 unverified lines**

Checked all pre-2026-09-04 lines (1-45 and 237-260 per git blame -L 1,262) for checkable claims. Found clean: (1) the Loop/Context/Harness/Fleet Engineering table content (docs/21:242-247) — each discipline's one-line gloss matches Cobus Greyling's source article's own characterization (loop = the system that decides/verifies, context = what the model sees, harness = single-run reliability scaffolding, fleet = governed multi-agent management) closely enough to be a fair paraphrase, not a misrepresentation; (2) all six 'Related' section links (docs/21:257-262) resolve to real files that exist in docs/ (02-agent-loop-cycle.md, 13-context-management.md, 05-claude-md.md, 16-memory-patterns.md, 24-harness-patterns.md, 37-session-architecture.md); (3) the Cherny/Osmani 'loop primacy' framing (docs/21:10-13) is substantively accurate to Osmani's actual blog post content, just missing its citation link (see findings); (4) no version numbers, model IDs, prices, flags, or numeric statistics appear anywhere in the pre-09-04 lines — this is pure framing/attribution prose, so the checkable surface is citations and attributed positions, not platform facts (consistent with this being a Part I, tool-agnostic doc that doesn't need version stamps).

### `06` — docs/06-skills.md (Part II)

65 previously-unverified lines · 9 claims checked · 6 URLs opened · 2 findings · **13.8 claims per 100 unverified lines**

Checked and found CORRECT: (1) Lines 1-4 and 11-12 — \"Skills are on-demand procedures stored in `.claude/skills/<name>/SKILL.md`\" and \"Commit skills to git... Claude auto-discovers them\" — confirmed against the live official docs (curl https://code.claude.com/docs/en/skills.md, fetched 2026-09-07): matches current path structure, frontmatter fields (name/description), and the \"loads only when it's used\" on-demand behavior. This is a stable architectural fact, not a version-sensitive one — confirmed by precedent: docs/03-*.md line 182 states the identical `.claude/skills/<name>/SKILL.md` fact with no version stamp either, so this doc is not a defect against the Part II version-stamp promise. (2) Lines 15-19 and 41-48 — the example SKILL.md frontmatter (`name:`, `description:`) and generic deploy/implement-feature skeletons are illustrative, not verbatim excerpts from any real skill, so they carry nothing to fact-check beyond frontmatter-field validity, which I confirmed against the docs' own frontmatter reference. (3) Line 65 — Simon Willison's \"shot-scraper video\" quote (\"it works kind of like bundling a SKILL.md file directly inside the tool\") verified word-for-word against the live page (HTTP 200) and correctly attributed/linked/dated (Jun 30, 2026 post). (4) Lines 69-75 — mimeo (arXiv 2609.00453) description verified against the live abstract: author (Timothy Kassis), the extraction/verification-against-source mechanism, and the \"gains clearest on obscure quotation-heavy questions\" claim all check out precisely (matches \"Knowledge access was clearest: mimeo answered all 20 obscure, quotation-heavy questions; no closed-book condition answered more than 10\"); the KB correctly does NOT extend this to the paper's unsupported persona/judgment-transfer claims. (5) Lines 78-86 — SkillZip Pro (arXiv 2608.30785) stats verified against the live abstract: \"38%\" skill-bundle token reduction and \"10.4%\" per-run token reduction with \"no quality loss\" match the abstract's \"removes 38% of skill bundle tokens and 10.4% of end-to-end per-run tokens with no quality loss\" exactly; authors, submission date (31 Aug 2026), and title all correct and properly linked. (6) All four external references in this file were checked for hyperlink compliance (rule 7); three of four are correctly linked (Willison, mimeo, SkillZip Pro).

### `31` — docs/31-claude-tag.md (Part II)

62 previously-unverified lines · 14 claims checked · 9 URLs opened · 4 findings · **22.6 claims per 100 unverified lines**

Verified CORRECT and exact-match against live primary sources: (1) the Karpathy quote at lines 40-44 — fetched the actual tweet (id 2069547676849557725) via Twitter's syndication/fxtwitter APIs (the oembed/legacy API truncates this note-tweet, but fxtwitter returned the full text) and confirmed the KB's quoted text is a verbatim, correctly-truncated excerpt, correctly attributed to Andrej Karpathy, correctly dated Jun 2026 (actual tweet date: 2026-06-23), and is a genuine reply/quote-tweet of Anthropic's own Claude Tag announcement tweet. (2) Line 57's "65% of Anthropic's product team's code is created using their internal version of Claude Tag" — verbatim match against the live anthropic.com/news/introducing-claude-tag page. (3) Line 58's "Powered by Claude Opus 4.8" — verbatim match ("Claude Tag works with Opus 4.8") on the same page; this line itself satisfies Part II's version-stamp promise, no ADD_VERSION_STAMP needed. (4) Line 59's Anthropic quote — verbatim match on the same page. (5) Line 7's citation (title "Introducing Claude Tag", Jun 2026) — confirmed via the live page's own dateline "Jun 23, 2026". (6) Lines 11-14 (channel-scoped identity), 23-26 (self-scheduling/ambient behavior), 28-31 (isolated sandbox per invocation), and 33-36 (governance: spend limits, audit logging, role-based memory) all corroborated against code.claude.com/docs/en/claude-tag and claude.com/product/tag. (7) Lines 69-81 (the newer Production Deployment / on-call section: 14-minute median, 4-minute fastest, lessons.md mechanism, and the verbatim "query the data first, then theorize" quote) all confirmed verbatim against the live claude.com/blog/ai-ci-cd-on-call page, with datePublished meta confirming Aug 2026 — only that citation's article TITLE was wrong (see finding). (8) Line 89's `--bg` flag for background agents — confirmed current and real via the live claude-code CHANGELOG.md (dozens of recent entries). No bare/unlinked repo slugs (rule 7) found in this line range.

### `19` — docs/19-mcp-security.md (Part II)

61 previously-unverified lines · 12 claims checked · 11 URLs opened · 5 findings · **19.7 claims per 100 unverified lines**

Checked and found accurate/unproblematic: (1) the AgentJacking/Sentry-DSN attack narrative (lines 7-26) — matches thenewstack.io/agentjacking-sentry-mcp-attack/ (live, HTTP 200, published 2026-06-21T17:00:00Z by Janakiram MSV) on every checkable point: DSN-as-public-write-only-credential mechanism, 'Claude Code, Cursor, and Codex' as the three named affected agents, 'Sentry MCP server' as the exact delivery mechanism phrase, and the RCE/credential-exposure consequence. (2) The 'no reliable way to distinguish real tool output from attacker-injected output' and 'auto-mode permission classifiers review tool calls, not tool results' claims (lines 33/35) — consistent with the article's own reporting that prompt-layer/system-prompt defenses did not stop the agents, and with the current Claude Code hooks guide's PermissionRequest event description ('When a tool call needs a permission decision'). (3) PreToolUse/PostToolUse as real, current Claude Code hook event names (line 42) — confirmed against the live hooks guide. (4) The Sources citation at line 61 — already a proper markdown hyperlink, title and Jun 21, 2026 date both match the live article exactly. (5) The article URL itself — live, HTTP 200, no dead-source issue. Ran `git blame -L 1,61` — confirmed all 61 lines are a single 2026-06-21 commit (5ab21913), so every line was in scope as pre-2026-09-04 unverified content; none postdate that boundary.

### `17` — docs/17-failure-patterns.md (Part I)

34 previously-unverified lines · 23 claims checked · 14 URLs opened · 2 findings · **67.6 claims per 100 unverified lines**

Verified against primary sources, all clean, spanning both the pre-2026-09-04 lines (git-blame priority) and a few newer ones: (1) Lines 1-13 (intro taxonomy: underspecification/capability/harness errors) — confirmed word-for-word consistent with the actual ClayBuddy paper (arXiv 2606.19380, 'ClayBuddy: A Framework, Evaluation, & Mitigation of Coding Agent Failures', dated 2026/06/13 per arXiv metadata — matches the KB's 'Jun 2026' and the taxonomy in the abstract). (2) Lines 16-20, 38-42, 44-45 (Correction loop, Infinite exploration, No stopping condition, Uncapped headless run, Reviewer bias, Polling loop, Provider lock-in, Orchestration tax, Dark factory) — general engineering advice with no unsupported factual claims beyond the $47k line flagged above; `--max-turns` and `--max-budget-usd` both confirmed live, current flags in the official CLI reference (code.claude.com/docs/en/cli-reference) with matching behavior description (print-mode only, budget cap covers subagent spend, 'Budget limit reached' error). (3) Line 45's and line 58's internal links (docs/14-human-in-the-loop.md, docs/34-loop-patterns.md) and line 46's anchor (docs/27-loop-contract.md#stop-condition-taxonomy) all resolve to real files/sections in the repo. (4) Line 49 (Context drift / omnigent-ai/omnigent) — repo exists (public, default_branch main, 9.7k stars), and `detect_task_switch` is a real symbol found via gh api search/code in omnigent/policies/builtins/context.py — the KB's description of the mechanism matches. (5) Lines 52-58 (State rot, Verifier theater, Notification fatigue, Infinite fix loop, Fixing flakes with code, Over-Reach, Parallel Collision, all citing cobusgreyling/loop-engineering) — repo exists (default_branch main) and every specific mechanism checked against the repo's own docs matched closely: verifier 'default stance REJECT' (docs/anti-patterns.md #1), 3-attempt hard cap (#2), state rot / STATE.md schema (#5), fixing flakes with code / quarantine (#8), path denylist enforced by the loop-constraints skill reading safety.md 'at the start of every loop run' (docs/safety.md), notification fatigue / notify-on-delta (docs/failure-modes.md), and acting_on-field collision detection with worktree isolation (stories/multi-loop-collision.md). (6) Line 61 (Zombie finding / gomilesf/convergo) — repo exists (default_branch main); its README.md contains the exact 'adjudication ratchet' ('Findings already adjudicated invalid cannot re-block a later round without new evidence') and 'fresh reviewer' exit-gate language the KB paraphrases, plus the '3 rounds without a clean fresh pass' cap matching the KB's escalation description. (7) Spot-checked one non-priority claim (line 43's Trio-library 50%/67% Anthropic-study figure and the 'code vending machine' quote) against Osmani's 'Agentic Skill Decay' (addyo.substack.com) — both confirmed verbatim-accurate and correctly linked. No bare (unlinked) repo/tool slugs found anywhere in lines 1-65 other than the two findings above — every other external reference in range is a proper markdown hyperlink (rule 7 otherwise satisfied throughout this unit).

### `18` — docs/18-quick-reference.md (Part II)

33 previously-unverified lines · 11 claims checked · 45 URLs opened · 1 findings · **33.3 claims per 100 unverified lines**

Verified accurate and current, no changes needed: `--permission-mode auto` (valid value, in the Shift+Tab cycle since v2.1.111 per cli-reference.md); `--max-turns 20` on `-p` (documented "print mode only", works as shown); `--allowedTools "Read,Edit"` comma-separated syntax (confirmed against headless.md's own examples `--allowedTools "Read,Edit,Bash"` and `--allowedTools "Bash,Read,Edit"`); `--output-format json | jq '.result'` (headless.md: "Claude Code prints the review as JSON, with the text in the result field"); `claude --continue` / `claude --resume` (match cli-reference.md's `--continue, -c` and `--resume, -r` descriptions); `/clear` (matches commands.md: "Start a new conversation with empty context"); `/compact Preserve: ...` (matches commands.md: `/compact [instructions]` — "Optionally pass focus instructions for the summary"); `/goal all tests pass and build succeeds` (matches commands.md: `/goal [condition|clear]` — confirmed a real native Claude Code command, distinct from the third-party Grok-CLI /goal that docs/30 also discusses). Lines 35-50 (the already-verified 2026-09-05 gotcha table) got one spot-check: ran `claude --help` locally (v2.1.263 installed) and confirmed `--max-turns` is indeed absent from the listing while `--max-budget-usd` and `--permission-prompts` are present, matching the table's framing. No bare/unlinked external references (rule 7) in this range — the only names are the `claude` CLI itself and two internal doc cross-links (29-background-agents.md, 09-headless-mode.md), already proper markdown links. No absence claims, no dead-source quotes, no fabricated flags/env vars found in this range.

### `24[1001-1172]` — docs/24-harness-patterns.md (Part I)

32 previously-unverified lines · 14 claims checked · 25 URLs opened · 4 findings · **43.8 claims per 100 unverified lines**

Checked and found CORRECT within lines 1001-1172: (1) ClaudeWarp CHANGELOG.md quotes 'no Harness row is yet natively superseded' (0.35.0, 2026-06-30 run) and 'No harness row is superseded' (0.42.0, 2026-09-04 run) — both verbatim-match the live raw CHANGELOG.md. (2) docs/reference/architecture.md quote 'No Harness row became native.' — verbatim match, line 82 of the live file. (3) The v2.1.198 'claude --bg began rejecting -p' claim (worded as a Fixed bullet) — verbatim-confirmed in the real anthropics/claude-code CHANGELOG.md under ## 2.1.198: 'Fixed `claude --bg` silently creating an unattachable session when combined with `--print`/`-p`; the conflicting flags are now rejected up front.' (4) The v2.1.222 '\"Removed ultraplan feature.\"' quote — confirmed as the literal last bullet under ## 2.1.222 in the real changelog (both today's and the 2026-09-04-dated commit). (5) The claude-warp-spec-refine 'superseded by another ClaudeWarp skill, not a native feature' claim — confirmed in ClaudeWarp's CHANGELOG.md: 'superseded by `/claude-warp-contract`'. (6) 'v2.1.101' as the earliest confirmed /ultraplan presence — confirmed via the real changelog (## 2.1.101 mentions /ultraplan auto-creating a cloud environment) and the whats-new/2026-w15 digest, which documents its April 2026 launch. (7) '53 releases read in full' for the v2.1.200 → v2.1.261 window — confirmed by direct header count against the changelog dated exactly 2026-09-04 (53 releases in the inclusive range). (8) eugenelim/agent-ready-repo and lucagattoni/Claude-Warp both exist, are public, and default to `main` (checked via `gh api` before any raw-file fetch, per rule 4). (9) Citation-link hygiene (rule 7): every external reference in the 1001-1172 range (eugenelim/agent-ready-repo x2, Claude-Warp CHANGELOG.md and architecture.md, four deeplearning.ai/the-batch Andrew Ng articles, the official changelog, /docs/en/commands) is a proper markdown hyperlink — no bare names or bare owner/repo slugs found.

### `15` — docs/15-explore-plan-implement.md (Part II)

25 previously-unverified lines · 17 claims checked · 4 URLs opened · 0 findings · **68.0 claims per 100 unverified lines**

All 79 lines checked, with priority on the 25 pre-2026-09-04 lines identified via `git blame` (lines 1-6, 8-26, commit 100ec833, 2026-06-21) plus line 7 (2026-09-04) and lines 27-79 (2026-09-06, commit 29519f0c). Classes verified: (1) CLI flags/keybindings — `claude --permission-mode plan`, `Ctrl+G` (opens plan in default text editor), `Shift+Tab` cycle order `default→acceptEdits→plan→default` — all confirmed live against code.claude.com/docs/en/permission-modes. (2) Two attributed block quotes (lines 35-37 and 41-42) — both verified verbatim, character-for-character, against the live page, correctly attributed and anchored. (3) Built-in subagent behavior claims (Explore/Plan skip CLAUDE.md and git status) — confirmed verbatim against both docs/07-subagents.md and the live code.claude.com/docs/en/sub-agents page. (4) Version claim v2.1.198 for Explore's model-inheritance change — confirmed against the live claude-code CHANGELOG.md (## 2.1.198 section). (5) All internal doc links and anchors (07-subagents.md#built-in-subagent-types, 36-development-workflow.md#the-spec-scales-with-the-risk-not-with-the-task, 08-permissions.md, 35-choosing-your-mode.md) resolve to real files/headings. (6) External-reference hyperlink rule (rule 7) — both external refs are proper markdown links, no bare names. (7) Part II version-stamp promise — confirmed via plans/20260904_2053-open-work-backlog.md (H1 row) that docs/15's stamp obligation is already closed (2026-09-06, v2.1.198 marker at line 66); did not re-raise since the doc's other CLI facts (--permission-mode plan, Ctrl+G) predate the visible CLI changelog range and cannot be dated to a specific introducing version.

### `35` — docs/35-choosing-your-mode.md (Part II)

0 previously-unverified lines · 19 claims checked · 10 URLs opened · 4 findings · **1900.0 claims per 100 unverified lines**

All lines in this doc were authored 2026-09-04/09-06 (git blame -L 1,175 shows commits a2432307 and 6619d495 only) — no pre-2026-09-04 content exists, so nothing was already covered by the earlier pass. Verified clean: (1) both Andrew Ng quotes (lines 19-23 "a complex, highly iterative process"/"high-skill judgement"; lines 132-135 "Setting up competitions...") — fetched both deeplearning.ai articles via curl, both bolded fragments match verbatim, both cited dates (2026-09-04, 2026-08-07) match each article's own datePublished metadata; (2) the Anthropic quote (lines 25-28) — fetched anthropic.com/engineering/multi-agent-research-system, quote matches verbatim, "Jun 13, 2025" on page matches cited 2025-06-13; (3) the $0.50/query and $3.00/10-min figures (line 138) match the Ng source exactly; (4) the compound-probability math (line 113): 0.9^10 = 0.34868 ≈ 0.35, correct; (5) the "~6 weeks" Pinakes case-study cross-reference (line 30) matches docs/37's own "~6 weeks old" (docs/37:161); (6) the ClaudeWarp repo (line 155) exists, is public, and its README substantively supports "durable state in git, budget caps on every scaffold, independent checkers" (line 156); (7) the `/claude-warp-sync` skill (line 157) exists at skills/claude-warp-sync/SKILL.md and its description ("prune harness components that have become native") supports "retire its own components"; (8) the "retired nothing" claim (line 161) is corroborated by docs/24's own #when-to-remove-harness section; (9) all three internal anchors used (docs/37#case-study-pinakes, docs/24#when-to-remove-harness, docs/36#a-worked-reference-implementation) exist; (10) every external reference in the doc (2 Ng links, 1 Anthropic link, 1 ClaudeWarp link) is already a markdown hyperlink — no rule-7 violations; (11) all 16 internal doc links used in this file resolve to real files; (12) the `/goal` description (line 98, "independent per-turn evaluator") matches the official code.claude.com/docs/en/goal page verbatim in substance; (13) the `/loop` description (line 102, "needs a session open") matches the same official page's comparison table; (14) the `/usage` per-loop-breakdown description (lines 142-143) matches CHANGELOG.md v2.1.243 verbatim in substance.

